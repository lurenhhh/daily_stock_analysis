# 开发文档 · 原始财报查询（Raw Filings Lookup）

> 版本：v0.2（据反馈调整：查看原文改为「跳转官方源、贴近原始文档」，去除强制 PDF 代理）
> 日期：2026-08-25
> 上游：《功能文档-02-原始财报查询》
> 适用工程：后端 `api/` + `data_provider/` + `src/services/`；前端 `apps/dsa-web`
> 开发层级：Layer 3，不依赖账号（L2），匿名可用

---

## 0. 已确认的产品决策（本文档据此展开）

| 决策项 | 结论 |
|--------|------|
| 首发市场 | **A股 + 港股**（美股放后续） |
| 查看原文方式 | **跳转官方源、打开原始财报文档**（贴近原文，接受新标签跳转）；不做强制内嵌代理 |
| 估值页联动 | **本期做**：估值页 →「查看当期财报」跳转 |
| AI 摘要按钮 | **本期预置**（灰置/禁用），为 L5 铺路 |
| 缓存策略 | 由本文档给默认方案（第 6 节），上线前可调 |

> v0.1 → v0.2 变更说明：原方案为「内嵌 PDF + 后端代理」。据反馈改为**以跳转官方源、呈现原始文档为主**，因此**去掉强制的 PDF 代理、防盗链与防 SSRF 复杂度**，工作量与风险显著下降。内嵌预览降级为**可选增强**（见第 5 节）。

---

## 1. 架构总览

```
前端 FilingsPage(/filings)
   │  搜索(StockAutocomplete) → 报告列表 → 「查看原文」跳官方源 / 「AI摘要」(灰置)
   ▼
后端 /api/v1/filings
   └─ GET /filings   列表：按 code+market 查定期报告清单（带缓存），返回官方源链接
   ▼
data_provider/filings_fetcher.py   (新增，继承 base 抽象)
   ├─ A股  → AkShare / 巨潮 cninfo
   └─ 港股 → HKEXnews 披露易（见第 4.2 风险）
   ▼
src/services/filings_service.py    (新增：编排 + 清单缓存)
```

设计原则：**只做"目录 + 指路"**——聚合官方定期报告清单，点击后**直接打开官方原始文档**。我们不托管、不加工、不代理 PDF，最大程度贴近原文、且合规最干净、工作量最小。

---

## 2. 数据模型（返回结构，不入业务库）

```ts
type FilingMarket = 'ashare' | 'hk';
type FilingType = 'annual' | 'interim' | 'q1' | 'q3' | 'other';

interface FilingItem {
  id: string;             // 稳定标识（源公告 id 或 hash(url)）
  code: string;
  displayCode: string;
  market: FilingMarket;
  title: string;          // 如「2025 年年度报告」
  reportType: FilingType;
  reportPeriod: string;   // "2025" / "2025H1" / "2025Q3"
  publishDate: string;    // YYYY-MM-DD
  officialUrl: string;    // ★ 官方原文链接（PDF 直链或官方详情页）——点击即跳转
  source: string;         // "巨潮资讯网" / "HKEXnews"
}

interface FilingsResponse {
  code: string;
  displayCode: string;
  market: FilingMarket;
  supported: boolean;
  message: string | null;
  items: FilingItem[];
}
```

> 关键字段是 `officialUrl`：优先给**原始 PDF 直链**（最贴近原文）；拿不到直链时退回官方详情页（`sourceUrl` 语义合并进 `officialUrl`）。

---

## 3. 后端 API 设计

挂在现有 `/api/v1` 下，新增 `filings` router（仿 `valuation` 注册方式）。匿名接口。

### 3.1 报告列表（唯一必需接口）

```
GET /api/v1/filings?code={code}&market={ashare|hk}&type={all|annual|interim|q1|q3}&year={all|2025...}
-> 200 FilingsResponse
```

- `market` 可由后端按 code 规则自动判定（复用 `stock_code_utils`），也允许显式传入。
- `type` / `year` 可选筛选；缺省返回全部（列表量不大，前端也可本地筛）。
- 不支持的市场：`supported=false` + message（前端走 `unsupported` 空态）。

> 本期**不需要** PDF 代理接口。`officialUrl` 由前端直接以新标签打开。

### 3.2 （可选，后续）PDF 代理

仅当将来要做**站内内嵌预览**时才需要，本期不做。届时再引入 `GET /filings/pdf?url=`，并配套**官方域名白名单（防 SSRF）** + 流式透传 + 限流。列为 v3 增强，非当前范围。

---

## 4. 数据源实现

### 4.1 A股（巨潮 cninfo，成熟）

- `data_provider/filings_fetcher.py` 新增 A股实现，复用现有 **AkShare** 拉取巨潮定期报告清单（AkShare 有巨潮公告/定期报告查询接口，**具体函数名以当前版本为准**），映射为 `FilingItem[]`。
- 只取**定期报告**（年报/半年报/一季报/三季报），过滤其他公告类型；`reportType` / `reportPeriod` 归一化。
- `officialUrl` 优先取巨潮的 PDF 直链。

### 4.2 港股（HKEXnews 披露易，⚠️ 风险点）

- **风险**：AkShare 对港股**定期报告清单**覆盖不如 A股稳定，可能需直接对接 **HKEXnews 披露易**公开检索。
- 因为本期只需拿到**清单 + 官方链接**（不内嵌、不代理），港股实现难度比 v0.1 低很多——**只要能列出报告并给出官方 `officialUrl` 即可**。
- 建议预研先确认 AkShare 港股可用性；不足则实现轻量的披露易官方源适配（只读公开数据、加缓存降频、遵守其条款）。
- 降级：若港股清单短期不稳，先上 A股，港股随数据源就绪再开。

### 4.3 抽象

- `FilingsFetcher(base)`：`fetch_filings(code, market, ...) -> list[FilingItem]`，按 market 分发，风格与现有各 fetcher 一致。

---

## 5. 前端「查看原文」交互

- 每条报告的「查看原文」= **在新标签打开 `officialUrl`**（`target="_blank"` + `rel="noopener noreferrer"`），直达官方原始文档。
- 同一入口即"查看"也即"下载"（官方 PDF 打开后浏览器自带下载）。
- 显著标注 `source`（来源）与「原文来自官方披露平台」的说明，强化"贴近原文、非二次加工"。
- **可选增强（后续）**：站内内嵌预览（需第 3.2 代理），本期不做。

---

## 6. 缓存策略（本文档给定默认）

| 对象 | 缓存键 | TTL（建议默认） | 说明 |
|------|--------|:---:|------|
| 报告清单 | `filings:{market}:{code}` | **12 小时** | 财报季可临时缩到 2–4h；平时 12–24h 足够 |

- PDF 不经过我们（直接跳官方源），**无需缓存 PDF**。
- 存储：MVP 复用现有存储/内存缓存即可，不上 Redis（零成本原则）。
- 失败降级：数据源超时返回上次缓存（stale-while-revalidate）或明确错误态。
- 可选「刷新」按钮绕过缓存。

---

## 7. 前端设计（`apps/dsa-web`）

### 7.1 新增文件

```
src/
├── api/filings.ts                    # 列表请求
├── pages/FilingsPage.tsx             # 新页面
└── components/filings/
    ├── FilingList.tsx                # 列表 + 类型/年份筛选 + 跳转官方源
    └── AiSummaryButton.tsx           # AI 摘要按钮（本期 disabled 灰置）
```

> 相比 v0.1 去掉了 `FilingViewerModal`（内嵌查看器），前端更轻。

### 7.2 路由与导航

- `App.tsx` 新增 `<Route path="/filings" element={<FilingsPage />} />`（lazy，仿现有页面）。
- 「分析」区导航加入口，与 `估值分析`、`我的看板` 平级。

### 7.3 页面结构

- 顶部：`StockAutocomplete` + 类型分段按钮（全部/年报/半年报/季报）+ 年份筛选。
- 主体：`FilingList`（复用 `Card`/`EmptyState`/`ApiErrorAlert`）。
- 每行：标题、类型标签、报告期、披露日期、`来源` + 操作「查看原文(跳官方源)」「AI 摘要(灰置)」。

### 7.4 AI 摘要按钮位（灰置）

- `AiSummaryButton` 渲染为 `disabled`，tooltip「AI 摘要即将上线」。
- 预留 props（`filingId` / `officialUrl`），L5 上线接后端摘要接口即可点亮。

---

## 8. 估值页 → 当期财报 联动

- `ValuationPage` 财务图表附近增加「查看财报原文」入口。
- 跳 `/filings?code={dashCode}&market={市场}`（可带 `year` 预选当期）。
- 实现：`react-router` 携带 query；`FilingsPage` 读取 query 自动查询与筛选。

---

## 9. 合规设计

- 仅呈现**依法公开披露**的定期报告，纯客观、不加观点、不荐股。
- **不托管、不加工、不代理** PDF，点击直达官方源，天然贴近原文且合规最干净。
- 每条报告标注 `source`；页面保留「不构成投资建议」免责。
- AI 摘要（L5）严守：只做客观摘要，不做判断/建议。
- （若将来做内嵌代理，再补域名白名单/防 SSRF——本期不涉及。）

---

## 10. 边界与异常

- 市场不支持 / code 非法 → `supported=false` 空态。
- 数据源超时/无结果 → 错误态 + 重试（`ApiErrorAlert`），或返回 stale 缓存。
- `officialUrl` 缺失 → 该条禁用「查看原文」并提示。
- 港股数据质量不足 → 按 4.2 降级。
- 免费源限流 → 缓存 + 退避重试。

---

## 11. 分期交付

| 子阶段 | 内容 |
|------|------|
| **v1** | A股定期报告：列表 + 筛选 + 跳转官方原文 + 估值页联动 + AI 摘要按钮位（灰置） |
| **v1.1** | 港股（HKEXnews；本期只需清单+官方链接，难度较低） |
| v2 | 接入 AI 财报摘要（L5），点亮按钮 |
| v3 | 站内内嵌预览（PDF 代理）；临时公告/招股书；报告全文检索；美股 EDGAR |

---

## 12. 测试用例

- 输入某 A股公司 → 返回历年定期报告，字段正确、按披露日期倒序。
- 类型/年份筛选生效。
- 点「查看原文」→ 新标签打开官方原始 PDF/详情页。
- 从估值页「查看当期财报」跳转 → 落到 filings 页并自动查询该公司。
- AI 摘要按钮为灰置且有提示。
- 港股：至少能出列表并跳官方源。
- 数据源超时 → 错误态或返回缓存，不白屏。
- `officialUrl` 缺失的条目 → 查看按钮禁用。

---

## 13. 开发任务清单（建议顺序）

1. 后端 `data_provider/filings_fetcher.py`：A股（AkShare/巨潮）实现 + `FilingItem` 映射。
2. 后端 `src/services/filings_service.py`：编排 + 清单缓存（12h）。
3. 后端 `api/v1/endpoints/filings.py` + schema + 注册 router：`GET /filings`。
4. 前端 `api/filings.ts` + `FilingsPage` + `FilingList`（含跳转官方源）+ 路由/导航。
5. 前端 `AiSummaryButton`（灰置占位）。
6. 估值页联动入口 + query 解析。
7. 港股实现（按 4.2 结论）。
8. 合规文案 + 测试用例回归。

> 相比 v0.1 去掉了「PDF 代理接口」和「内嵌查看器」两项，工期更短。

---

## 14. 待确认问题

1. **港股数据源**：预研确认 AkShare 港股定期报告覆盖度；不足时是否投入做披露易官方源适配？（本期只需清单+链接，成本已比内嵌方案低）
2. `officialUrl` 优先给**PDF 直链**还是**官方详情页**？（直链更贴近原文，建议优先直链、拿不到再退详情页）
3. 清单缓存 TTL 最终值（默认 12h 是否 OK；财报季是否自动缩短）？
4. 是否需要「刷新绕过缓存」按钮？
5. AI 摘要按钮位文案（「即将上线」/其他）。

---

*本功能为客观信息聚合，仅呈现公开披露的财报原文，不构成投资建议。代码片段为规范示意，具体实现以工程约定为准。*

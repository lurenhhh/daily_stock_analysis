# 开发文档 · 投资人视角模版（Investment Lens Templates）

> 版本：v0.1
> 日期：2026-08-25
> 上游：《功能文档-01-投资人视角模版》
> 适用工程：`apps/dsa-web`（React + TypeScript + react-router-dom + Tailwind）
> 开发范围：Layer 1 / 子阶段 1.0（MVP），无需后端、无需账号

---

## 1. 一句话目标

把「我的看板」的空白页，换成一个**投资人视角模版库**：用户选一个模版（如「护城河镜头」）→ 选一只股票 → 一键生成一组带方法论说明的指标看板。模版是**作用在用户已选股票上的观察镜头**，不是选股器。

---

## 2. 推荐做成什么样（产品形态与交互）

### 2.1 整体形态

模版功能**不新增独立主路由**，而是内嵌进现有 `/my-dashboard` 页：

- 看板**为空** → 页面主体显示「模版库」（Gallery），这是治冷启动的核心落点。
- 看板**非空** → 顶部 `PageHeader` 增加一个「从模版添加」按钮，点击唤起模版库（抽屉或模态）。

一个模版从选择到落地，走三步：**选模版 → 选股票 → 生成看板**。

### 2.2 关键界面（文字线框）

**① 模版库（Gallery）**

```
┌───────────────────────────────────────────────┐
│  投资人视角模版                                    │
│  选择一个视角，套用到你关心的股票上                    │
│                                                 │
│  ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │
│  │ 🏰 护城河镜头  │ │ 🔄 逆向低估镜头│ │ 💰 稳健分红 │ │
│  │ 看是不是好生意 │ │ 观察便宜但…    │ │ 关注现金回报│ │
│  │ ROE 毛利率 FCF │ │ PE分位 扣非 …  │ │ 股息率 FCF…│ │
│  │ [查看] [应用]  │ │ [查看] [应用]  │ │ [查看][应用]│ │
│  └─────────────┘ └─────────────┘ └───────────┘ │
└───────────────────────────────────────────────┘
```

- 每张卡片：图标 + 模版名 + tagline + 镜头标签（chips）+ 两个动作「查看详情 / 应用模版」。
- 卡片复用现有 `Card` 组件与设计变量（`rounded-2xl`、`border-border/70`、`bg-card/70` 等），与估值页视觉一致。

**② 模版详情（抽屉 Drawer 或模态 Modal）**

- 模版名、tagline、**方法论说明（philosophy）**（这是内容护城河，占显著位置）。
- 镜头清单：逐项列出「指标名 + 这个模版为什么看它（note）+ 客观提示（referenceHint）」。
- 底部固定「应用到股票」主按钮 + 「本内容不构成投资建议」免责小字。

**③ 应用流程（选股）**

- 点「应用模版」→ 弹出轻量选股模态，内嵌现有 `StockAutocomplete`。
- 用户选定一只股票 → 确认 → 批量生成 `DashboardItem` → 关闭模态 → 停留在看板页看到新生成的一组图表。
- 若模版镜头已存在于看板（同股票同图表），自动去重（`addDashboardItem` 已支持），并给出「已添加 N 项，跳过 M 项重复」的轻提示（toast/内联）。

### 2.3 v1 明确的取舍

- v1 只支持**单只股票**应用（多只对比列入 1.2）。
- v1 模版为**静态内置**，用户不可编辑模版参数（只能套用后在看板里删项）。
- v1 不做「保存我的模版 / 自定义模版」（依赖 Layer 2 账号）。

---

## 3. 架构与文件规划

遵循现有目录约定，新增文件如下（**均为新增，不改动既有业务逻辑**）：

```
apps/dsa-web/src/
├── data/
│   └── lensTemplates.ts            # 【新增】3个官方模版的静态配置 + 类型
├── utils/
│   └── applyLensTemplate.ts        # 【新增】把模版实例化为 DashboardItem 的逻辑
├── components/dashboard/           # 【新增目录】
│   ├── TemplateGallery.tsx         # 模版库（卡片网格）
│   ├── TemplateCard.tsx            # 单个模版卡片
│   ├── TemplateDetail.tsx          # 模版详情（抽屉/模态）
│   └── ApplyTemplateModal.tsx      # 选股并应用的模态
└── pages/
    └── MyDashboardPage.tsx         # 【改动】空状态接入 Gallery、页头加入口
```

> 复用而非新建：`StockAutocomplete`、`DraggableChartGrid`、`components/common`（`Card`/`PageHeader`/`EmptyState`）、`utils/myDashboard`（`addDashboardItem` 等）、`contexts/UiLanguageContext`（`useUiLanguage`）。

---

## 4. 数据模型

新增类型放在 `data/lensTemplates.ts`，**完全复用**现有 `DashboardChartKind` / `ValuationMetric`（来自 `utils/myDashboard` 与 `api/valuation`）。

```ts
import type { DashboardChartKind } from '../utils/myDashboard';
import type { ValuationMetric } from '../api/valuation';

/** 模版里的单个镜头：与股票无关，只描述"看哪个指标、怎么看" */
export interface LensSpec {
  chart: DashboardChartKind;        // 'pe' | 'fund' | MetricKey
  metric?: ValuationMetric;         // 仅 chart==='pe' 时使用
  years?: number;                   // 缺省用模版 defaultYears
  noteKey: string;                  // i18n key：该镜头在本模版里的说明
  referenceHintKey?: string;        // i18n key：客观参考提示（非买卖建议）
}

/** 投资人视角模版：一组有序镜头 + 方法论说明 */
export interface LensTemplate {
  id: string;                       // 'moat' | 'contrarian' | 'dividend'
  icon: string;                     // 展示用 emoji/图标名
  nameKey: string;                  // i18n：模版名（方法论命名）
  taglineKey: string;               // i18n：一句话简介
  philosophyKey: string;            // i18n：方法论说明
  referenceKey?: string;            // i18n：参考理念来源说明（放说明，不做商品名）
  lenses: LensSpec[];
  defaultYears: number;             // 10 | 20
}
```

> 文案统一走 i18n（存 key 而非硬编码中文），与项目现有 `t()` 机制一致，方便中英繁多语言。

---

## 5. 三个官方模版的静态配置（可直接落地）

```ts
// data/lensTemplates.ts
export const LENS_TEMPLATES: LensTemplate[] = [
  {
    id: 'moat',
    icon: '🏰',
    nameKey: 'lens.moat.name',
    taglineKey: 'lens.moat.tagline',
    philosophyKey: 'lens.moat.philosophy',
    referenceKey: 'lens.moat.reference',
    defaultYears: 20,
    lenses: [
      { chart: 'roe',           noteKey: 'lens.moat.note.roe',   referenceHintKey: 'lens.moat.hint.roe' },
      { chart: 'grossMargin',   noteKey: 'lens.moat.note.gm' },
      { chart: 'freeCashFlow',  noteKey: 'lens.moat.note.fcf' },
      { chart: 'fund',          noteKey: 'lens.moat.note.fund' },
      { chart: 'pe', metric: 'pe_ttm', noteKey: 'lens.moat.note.pe' },
    ],
  },
  {
    id: 'contrarian',
    icon: '🔄',
    nameKey: 'lens.contrarian.name',
    taglineKey: 'lens.contrarian.tagline',
    philosophyKey: 'lens.contrarian.philosophy',
    defaultYears: 20,
    lenses: [
      { chart: 'pe', metric: 'pe_ttm', years: 20, noteKey: 'lens.contrarian.note.pe' },
      { chart: 'deductedNetProfit',    noteKey: 'lens.contrarian.note.dnp' },
      { chart: 'debtRatio',            noteKey: 'lens.contrarian.note.debt' },
      { chart: 'dividendYield',        noteKey: 'lens.contrarian.note.div' },
    ],
  },
  {
    id: 'dividend',
    icon: '💰',
    nameKey: 'lens.dividend.name',
    taglineKey: 'lens.dividend.tagline',
    philosophyKey: 'lens.dividend.philosophy',
    defaultYears: 20,
    lenses: [
      { chart: 'dividendYield',  noteKey: 'lens.dividend.note.div' },
      { chart: 'freeCashFlow',   noteKey: 'lens.dividend.note.fcf' },
      { chart: 'debtRatio',      noteKey: 'lens.dividend.note.debt' },
      { chart: 'roe',            noteKey: 'lens.dividend.note.roe' },
      { chart: 'pe', metric: 'pe_ttm', noteKey: 'lens.dividend.note.pe' },
    ],
  },
];
```

> 所有 `chart` 值都在你现有的 8 类镜头范围内，**不需要任何新增数据接口**。

---

## 6. 核心逻辑：把模版实例化为看板项

```ts
// utils/applyLensTemplate.ts
import { addDashboardItem } from './myDashboard';
import type { LensTemplate } from '../data/lensTemplates';

export interface ApplyTarget {
  code: string;          // 规范化代码（StockAutocomplete 提供）
  displayCode: string;   // 展示代码；无则回退用 code
  name: string | null;
}

export interface ApplyResult {
  added: number;
  skipped: number;       // 因去重跳过的数量
}

/** 将模版套到一只股票上，批量写入现有看板（localStorage）。 */
export function applyLensTemplate(template: LensTemplate, target: ApplyTarget): ApplyResult {
  let added = 0;
  let skipped = 0;
  for (const lens of template.lenses) {
    const { added: ok } = addDashboardItem({
      code: target.code,
      displayCode: target.displayCode || target.code,
      name: target.name,
      chart: lens.chart,
      metric: lens.chart === 'pe' ? (lens.metric ?? 'pe_ttm') : undefined,
      years: lens.years ?? template.defaultYears,
    });
    ok ? added++ : skipped++;
  }
  return { added, skipped };
}
```

- 复用现有 `addDashboardItem`（自带去重、自带 `emitChange()` 触发 `MY_DASHBOARD_CHANGED_EVENT`），因此 `MyDashboardPage` 会**自动**收到变更并重新渲染，无需额外通信。

---

## 7. 组件拆分与 Props

```ts
// TemplateGallery.tsx
interface TemplateGalleryProps {
  onApply: (template: LensTemplate) => void;   // 触发应用（打开选股模态）
  onView: (template: LensTemplate) => void;    // 打开详情
}

// TemplateCard.tsx
interface TemplateCardProps {
  template: LensTemplate;
  onApply: () => void;
  onView: () => void;
}

// TemplateDetail.tsx（抽屉/模态）
interface TemplateDetailProps {
  template: LensTemplate | null;   // null 表示关闭
  onClose: () => void;
  onApply: (template: LensTemplate) => void;
}

// ApplyTemplateModal.tsx（选股 → 应用）
interface ApplyTemplateModalProps {
  template: LensTemplate | null;   // null 表示关闭
  onClose: () => void;
  onApplied: (result: ApplyResult) => void;  // 应用完成回调（用于 toast）
}
```

`ApplyTemplateModal` 内部：内嵌 `StockAutocomplete`（沿用估值页用法 `onSubmit={(code, name) => ...}`）→ 用户选定后调用 `applyLensTemplate(template, target)` → 回调 `onApplied(result)` → 关闭。

---

## 8. `MyDashboardPage` 的改动点（最小侵入）

只动两处，不碰现有数据加载逻辑：

1. **空状态**：把当前的 `EmptyState`（`items.length === 0` 分支）替换为 `<TemplateGallery ... />`。
2. **页头**：给 `PageHeader` 的 `actions` 增加一个「从模版添加」按钮（与现有「刷新」按钮并列），点击打开 Gallery（模态形式）。
3. 页面维护三个 UI 状态：`galleryOpen` / `viewingTemplate` / `applyingTemplate`，串起「库 → 详情 → 选股应用」三步。
4. 应用成功后用一个轻量 toast/内联提示展示 `ApplyResult`（已有则复用现有提示组件；无则可简单内联一行）。

> 现有 `useEffect`（按 `itemsKey` 拉数据）保持不变——模版写入后事件触发 → `items` 更新 → 自动重新拉数据渲染。

---

## 9. i18n 文案 key（新增到 `i18n/uiText.ts` 及各 `locales`）

需要新增的 key 命名空间：`myDashboard.templates.*` 与 `lens.*`。示例（中文，英/繁按现有流程补）：

| key | 文案（zh） |
|-----|-----------|
| `myDashboard.templates.title` | 投资人视角模版 |
| `myDashboard.templates.subtitle` | 选择一个视角，套用到你关心的股票上 |
| `myDashboard.templates.view` | 查看详情 |
| `myDashboard.templates.apply` | 应用模版 |
| `myDashboard.templates.addFromTemplate` | 从模版添加 |
| `myDashboard.templates.pickStock` | 选择一只股票 |
| `myDashboard.templates.applied` | 已添加 {added} 项，跳过 {skipped} 项重复 |
| `myDashboard.templates.disclaimer` | 本内容为观察镜头，不构成投资建议 |
| `lens.moat.name` | 护城河镜头 |
| `lens.moat.tagline` | 看一家公司是不是「好生意」 |
| `lens.moat.philosophy` | 好生意的共同特征，是长期稳定的高资本回报、健康的盈利质量，以及能持续产生现金。本镜头把这些客观指标放在一起，帮你观察一家公司的生意质地。 |
| `lens.moat.reference` | 本镜头参考了价值投资中关于「经济护城河」的理念。 |
| `lens.moat.note.roe` | 长期 ROE 反映资本回报效率。 |
| `lens.moat.hint.roe` | ROE = 净利润 / 净资产，是衡量盈利能力的常用客观指标。 |
| `lens.moat.note.gm` | 毛利率的高低与稳定性反映定价能力。 |
| `lens.moat.note.fcf` | 持续为正的自由现金流，反映「赚到的是真钱」。 |
| `lens.moat.note.fund` | 观察营收长期成长与市值的关系。 |
| `lens.moat.note.pe` | 在了解生意质地后，用估值分位观察当前定价处于历史什么位置。 |
| `lens.contrarian.name` | 逆向低估镜头 |
| `lens.contrarian.tagline` | 观察那些便宜、但基本面未必恶化的公司 |
| `lens.contrarian.philosophy` | 逆向视角关注估值处于历史低位的公司，但便宜本身不是理由，还需确认盈利与财务是否稳健。本镜头把估值分位与基本面健康度并排呈现，供你自行判断。 |
| `lens.contrarian.note.pe` | 观察当前估值处于历史 20 年的什么分位。 |
| `lens.contrarian.note.dnp` | 剔除非经常损益，看主业是否仍在真实盈利。 |
| `lens.contrarian.note.debt` | 评估财务安全边际，避免「便宜有便宜的原因」。 |
| `lens.contrarian.note.div` | 低估叠加分红，是逆向视角常关注的组合。 |
| `lens.dividend.name` | 稳健分红镜头 |
| `lens.dividend.tagline` | 关注现金回报与财务稳健 |
| `lens.dividend.philosophy` | 偏好现金回报的观察视角，关注分红的持续性、现金流的支撑能力与财务的稳健程度。 |
| `lens.dividend.note.div` | 当前股息率与其历史水平。 |
| `lens.dividend.note.fcf` | 分红的可持续性取决于是否有现金支撑。 |
| `lens.dividend.note.debt` | 高负债可能侵蚀分红能力。 |
| `lens.dividend.note.roe` | 长期资本回报的稳定性。 |
| `lens.dividend.note.pe` | 观察当前估值分位。 |

> 文案措辞已按合规要求：只陈述客观常识，无「买入/建仓/低于X可买」等择时表述。

---

## 10. 合规实现要点（编码时必须遵守）

- 模版**只能**在用户选定股票后作用于该股票；**不得**实现「用模版跑出一批股票的列表」。
- 详情页与选股模态**必须**展示免责声明 `myDashboard.templates.disclaimer`。
- 所有 `note` / `referenceHint` / `philosophy` 文案评审时按「是否出现买卖/择时导向」逐条过一遍。
- 模版名用方法论命名，不用投资人真名；理念来源只放 `reference` 说明字段。

---

## 11. 边界与异常处理

- **displayCode 缺失**：`StockAutocomplete` 的 `onSubmit` 目前提供 `(code, name)`；若拿不到 `displayCode`，用 `code` 兜底（`applyLensTemplate` 已处理）。后续可在 autocomplete 回传规范 `displayCode`。
- **重复应用**：靠 `addDashboardItem` 的 `dashboardItemKey` 去重；提示里回报 `skipped` 数。
- **某镜头无数据**：不在应用时处理——沿用 `MyDashboardPage` 现有逻辑，图表项自身显示 `itemNoData` 空态。
- **localStorage 不可用（隐私模式）**：`saveDashboardItems` 已 try/catch 静默；应用后看板可能不持久，属已知降级，Layer 2 云端同步后消除。
- **看板项过多**：一次套用 4–5 项属正常；如担心堆积，可在提示里说明可随时移除（非 v1 必需）。

---

## 12. 测试用例（建议）

- 空看板进入 → 显示 3 张模版卡片，无空白 EmptyState。
- 点「查看详情」→ 抽屉展示方法论说明 + 全部镜头 note + 免责声明。
- 「护城河镜头」应用到某 A 股 → 看板新增 5 项（roe/gm/fcf/fund/pe），标题含股票名。
- 对同一股票重复应用同模版 → 提示「已添加 0 项，跳过 5 项」。
- 应用后刷新页面 → 看板项仍在（localStorage 持久化）。
- 非空看板 → 页头「从模版添加」可再次唤起模版库。
- 切换语言（zh/en）→ 模版名、说明、note 均随 i18n 切换。
- 隐私模式下应用 → 不报错，当次可见（不强求持久）。

---

## 13. 开发任务清单（建议顺序）

1. `data/lensTemplates.ts`：类型 + 3 个模版静态配置。
2. `i18n/uiText.ts` + `locales/*`：新增第 9 节所有 key（先补 zh，其余可占位）。
3. `utils/applyLensTemplate.ts`：实例化逻辑 + 单元测试。
4. `components/dashboard/TemplateCard.tsx` + `TemplateGallery.tsx`。
5. `components/dashboard/TemplateDetail.tsx`（抽屉/模态）。
6. `components/dashboard/ApplyTemplateModal.tsx`（内嵌 StockAutocomplete + 调用 apply）。
7. `pages/MyDashboardPage.tsx`：接空状态 Gallery + 页头入口 + 三态串联 + 应用提示。
8. 联调：三步流程 + 去重提示 + i18n 切换。
9. 合规文案过审（第 10 节）。
10. 按第 12 节测试用例回归。

---

## 14. 验收标准

- 新用户进入空看板即见模版库，可在 3 次点击内（选模版→选股→确认）得到一套看板。
- 3 个模版均能正确实例化为对应镜头，去重正常，i18n 完整。
- 全流程无新增后端依赖、无需登录即可使用。
- 所有涉及投资的文案通过合规检查，免责声明到位。

---

## 15. 后续（超出本期）

- 1.2：一个模版套多只股票 / 同模版横向对比视图。
- 1.3：「把当前看板存为我的模版」（需 Layer 2 账号 + 云端）。
- 未来：UGC 模版市场（分享/发现）。
- 埋点：模版应用率、来源模版留存（建议给 `DashboardItem` 增加可选 `sourceTemplateId` 字段以支持统计）。

---

*本文档为实现级设计参考。所有代码片段为规范示意，具体实现以工程约定为准；所有投资相关呈现均以「观察镜头、不构成投资建议」为前提。*

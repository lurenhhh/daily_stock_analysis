# 开发任务 · 持仓底稿云端同步（照搬看板同步模式）

> 版本：v0.1
> 日期：2026-08-27
> 目标：把「持仓底稿」（持仓纪律，现存 `localStorage`）同步到服务端，实现跨设备、防丢失
> 原则：**完全照搬现有"看板同步"的实现模式**，最小改动、零新范式

---

## 1. 背景

- 账号 + 看板同步已落地：后端 SQLite（`users` / `user_dashboard` 表）+ `/api/v1/account/*`，前端 `accountApi` + `UserAccountContext`。
- 持仓底稿（`utils/holdingDiscipline.ts`）目前**只存浏览器 `localStorage`**（key `dsa:holdingDiscipline`），换设备看不到、清缓存即丢——而它恰是用户最有价值的手写数据。
- 底稿是一个**整体 JSON 对象**（`Store`：holdings / reasons / logs / business / moat / members / metrics / mgmtCriteria），与看板的 JSON blob 模式一致，**可直接照搬**。

---

## 2. 改动总览（对照看板同步）

| 层 | 看板（已存在） | 持仓底稿（本次照做） |
|----|--------------|--------------------|
| DB 表 | `UserDashboard`（user_id, data_json, updated_at） | 新增 `UserDossier`（同结构） |
| Service | `DashboardService.get/save` | 新增 `DossierService.get/save` |
| 接口 | `GET/PUT /api/v1/account/dashboard` | 新增 `GET/PUT /api/v1/account/dossier` |
| 前端 api | `accountApi.getDashboard/putDashboard` | 新增 `getDossier/putDossier` |
| 前端本地层 | `replaceAllDashboardItems`（覆盖本地） | 新增 `replaceStore`（覆盖本地） |
| 同步编排 | `UserAccountContext`：登录拉取 + 变更去抖上推 + 首登迁移 | 照加一份持仓底稿的同步 |

> 唯一差异：看板 payload 是**数组** `items`；底稿 payload 是**对象** `store`（整个 Store）。默认值从 `[]` 改为 `{}`，校验从 `isinstance(list)` 改为 `isinstance(dict)`。

---

## 3. 后端改动

### 3.1 新增数据表（`src/storage.py`）

照 `UserDashboard` 加一张 `UserDossier`（按 users/user_dashboard 相同的建表/迁移方式落地）：

```python
class UserDossier(Base):
    __tablename__ = 'user_dossier'
    user_id = Column(Integer, ForeignKey('users.id'), primary_key=True)
    data_json = Column(Text, nullable=False, default='{}')
    updated_at = Column(DateTime, default=datetime.now, onupdate=datetime.now)
```

> 建表方式与 `users`/`user_dashboard` 保持一致（同一处 `create_all` 或同一套 `schema_migrations` 迁移登记），不要另起机制。

### 3.2 新增 Service（`src/services/account_service.py`）

照 `DashboardService` 加 `DossierService`，唯一区别是存"对象"而非"数组"：

```python
class DossierService:
    def __init__(self, db: Session):
        self.db = db

    def get(self, user_id: int) -> Tuple[dict, Optional[datetime]]:
        row = self.db.get(UserDossier, user_id)
        if not row:
            return {}, None
        try:
            store = json.loads(row.data_json) if row.data_json else {}
            if not isinstance(store, dict):
                store = {}
        except Exception:  # noqa: BLE001
            store = {}
        return store, row.updated_at

    def save(self, user_id: int, store: dict) -> datetime:
        payload = json.dumps(store or {}, ensure_ascii=False)
        row = self.db.get(UserDossier, user_id)
        now = datetime.now()
        if row:
            row.data_json = payload
            row.updated_at = now
        else:
            row = UserDossier(user_id=user_id, data_json=payload, updated_at=now)
            self.db.add(row)
        self.db.commit()
        return now
```

（记得 `from src.storage import ... UserDossier`。）

### 3.3 新增接口（`api/v1/endpoints/account.py`）

照 `get_dashboard`/`put_dashboard` 加两个，复用现成的 `get_current_user`：

```python
class DossierPutReq(BaseModel):
    store: dict = Field(default_factory=dict, description="持仓底稿 Store 原样")

@router.get("/dossier", summary="拉取当前用户持仓底稿")
def get_dossier(user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    store, updated = DossierService(db).get(user.id)
    return {"store": store, "updatedAt": updated.isoformat() if updated else None}

@router.put("/dossier", summary="覆盖保存持仓底稿（last-write-wins）")
def put_dossier(
    body: DossierPutReq,
    user: User = Depends(get_current_user),
    db: Session = Depends(get_db),
):
    updated = DossierService(db).save(user.id, body.store)
    return {"ok": True, "updatedAt": updated.isoformat()}
```

（import 处加上 `DossierService`。）

---

## 4. 前端改动

### 4.1 `api/account.ts`

照 `getDashboard`/`putDashboard` 加两个（`DossierStore` 从 holdingDiscipline 导出其 `Store` 类型）：

```ts
getDossier: async (): Promise<{ store: DossierStore | null; updatedAt: string | null }> => {
  const { data } = await accountClient.get('/api/v1/account/dossier');
  return data;
},
putDossier: async (store: DossierStore): Promise<{ ok: boolean; updatedAt: string }> => {
  const { data } = await accountClient.put('/api/v1/account/dossier', { store });
  return data;
},
```

### 4.2 `utils/holdingDiscipline.ts`

- 导出 `Store` 类型（供 api 引用），并新增一个覆盖本地的函数，照 `replaceAllDashboardItems`：

```ts
export type DossierStore = Store;

/** 用服务端底稿整体覆盖本地（登录后拉取）。 */
export function replaceStore(store: Partial<Store> | null | undefined): void {
  const safe: Store = { ...emptyStore(), ...(store && typeof store === 'object' ? store : {}) };
  saveStore(safe);   // saveStore 已触发 HOLDINGS_CHANGED_EVENT
}
```

- `loadStore` / `saveStore` / `HOLDINGS_CHANGED_EVENT` 均已存在，直接复用。

### 4.3 `contexts/UserAccountContext.tsx`（同步编排，照看板那份加一份）

在现有"看板同步"旁，照加"持仓底稿同步"，三件事：

1. **登录后拉取 + 首登迁移**（与看板逻辑同构）：
   ```
   const { store, updatedAt } = await accountApi.getDossier();
   const local = loadStore();
   const localNonEmpty = local.holdings.length > 0;
   if (!updatedAt && localNonEmpty) {
     await accountApi.putDossier(local);   // 首次登录：本地→云端迁移
   } else {
     replaceStore(store);                   // 否则以服务端为准覆盖本地
   }
   ```
2. **变更去抖上推**：监听 `HOLDINGS_CHANGED_EVENT`，登录态下去抖（如 800ms）`putDossier(loadStore())`。
   - 注意避免"拉取时的 `replaceStore` 触发事件又回推"造成回环——照看板现有做法处理（如同步期间设置抑制标志/比对内容）。
3. **登出**：与看板一致处理（保留本地或清空，按现有看板行为对齐即可）。

---

## 5. 边界与一致性

- **空判定**：底稿"服务端为空"= `updatedAt` 为 null 或 store 无 holdings；据此决定迁移还是覆盖。
- **回环防护**：`replaceStore` 会触发 `HOLDINGS_CHANGED_EVENT`，务必与看板同款抑制机制一致，防止拉取→事件→上推死循环。
- **隐私模式**：`saveStore` 已 try/catch 静默；同步失败不应阻塞页面。
- **敏感数据**：底稿含成本/持仓判断，属敏感个人数据；本次先跑通同步，加密/注销清除随 L2 整体合规一并补（见 L2 文档）。
- **冲突**：沿用看板的 last-write-wins，本期不做条目级合并。

---

## 6. 验收标准

- 登录后：A 设备录入的持仓底稿，B 设备登录同账号可见。
- 未登录时本地已有底稿，首次登录后迁移到服务端且不丢、不重复。
- 登录态下增删/编辑底稿，数秒内自动上推服务端（去抖生效、无回环刷屏）。
- 退出登录行为与看板一致。
- 隐私模式/接口失败不白屏、不阻塞。

---

## 7. 工作量

- 后端：1 张表 + 1 个 service 类 + 2 个接口，均为照抄，**约半天**。
- 前端：2 个 api + 1 个 `replaceStore` + `UserAccountContext` 同步一份，**约半天到 1 天**（主要是回环防护调试）。
- 合计约 **1 天**即可让持仓底稿具备跨设备同步。

---

*本任务仅新增持仓底稿的存储同步，不改动底稿功能本身；沿用现有账号与看板同步的全部约定。*

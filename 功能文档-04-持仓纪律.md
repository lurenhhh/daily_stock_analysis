# 功能文档 04 · 持仓纪律（结构化投资底稿）

> 版本：v0.3（新增「护城河评估」层，持仓详情页扩展为五层底稿）
> 日期：2026-08-25
> 上游依据：《产品规划-指标平台》Layer 6（投资第二大脑），因价值高可提前
> 开发层级：**L6**，零合规风险；MVP 可本地跑，完整价值依赖 L2 云端
> 定位：把"凭感觉持股"变成"照底稿持股"，并用买入理由约束卖出

---

## 1. 功能概述

用户维护一个**自己的持仓公司列表**，点进任一公司即进入其**结构化投资底稿**，自上而下五层：

1. **商业模式判断**（好处 / 坏处）
2. **护城河评估**（按经典护城河类型判断有无、宽窄与趋势）
3. **管理层评估**（对每位管理层，按 6 项标准判断是否符合）
4. **自定义财务指标**（用户挑选/添加自己关注的指标）
5. **买卖理由对比**（买入理由 vs 卖出理由；卖出前对质）

**一句话定位**：给你持有的每家公司写一份自己的研究底稿；卖出前，先和这份底稿对质。

> 版本沿革：v0.1 骨架（持仓+买入理由+卖出对质）→ v0.2 扩展为四层底稿 → **v0.3 新增「护城河评估」层，成为五层**。

---

## 2. 背景与目标

- **痛点**：散户亏钱多因情绪化，且持股缺乏结构化论证；想理性又没有承载工具。
- **留存护城河**：结构化底稿越厚，迁移成本越高——全产品最强留存资产。
- **零合规风险**：全是用户**自己的**判断，不给任何买卖建议。
- **契合价值观**：护城河、管理层评估都是价值投资的正统框架，和模版、目标人群同频。
- **差异点**：结构化护城河 + 管理层评估 + 投资底稿是主流工具都没有的东西。

---

## 3. 核心设计原则（贯穿全功能）

1. **渐进式、全部可选**（最重要）：结构很"重"，若逼用户填完才能用，多数人会中途放弃。建档只需**一句话商业模式判断**即可成立，其余板块折叠、想填再填、填一半也能存草稿。
2. **是仪式，不是强制锁**：拦不住也不该拦用户卖出（真实交易在券商 App）。卖出对质是**反思仪式**，把理由摆出来自我确认，不锁死。
3. **措辞永远中立**：只呈现用户**自己的原话与判断**，绝不替他判断"该不该买卖"。
4. **两头都砍，防死扛**：既拦冲动卖出，也不美化死扛；理由/标准破裂如实呈现。
5. **能自动带出的不让用户手填**：管理层名单从体检卡 `getLeaders` 自动带出；财务指标复用现有指标体系挑选，减少录入负担。
6. **清单式评估统一模版化**：护城河类型、管理层标准都以"默认清单 + 三态 + 可增删自定义"呈现，与"投资人视角模版"同一套思路。

---

## 4. 目标用户与场景

- **认真做研究的价值投资者 / 进阶散户**：愿意为持仓写底稿、按逻辑持有。
- 诚实取舍：这是**深度绑定核心用户**的功能，不是拉新大众的功能（拉新靠模版与 AI 摘要，绑人靠底稿）。
- 场景：建仓一家公司 → 写下商业模式、评估护城河与管理层、挂上关注指标、记录买入理由 → 日后想卖 → 打开底稿逐条对质 → 冷静决策并留档复盘。

---

## 5. 页面与信息架构

- **持仓公司列表页**：用户添加/管理自己持有的公司；每项显示公司名、简要状态（底稿完成度、是否有理由破裂标记）。
- **持仓详情页（投资底稿）**：点进某公司 → 五层结构（第 6 节）。
- 建议**挂在现有持仓（Portfolio）体系下**，复用持仓数据、避免导航膨胀（详见待确认第 1 条）。

---

## 6. 持仓详情页结构（五层·核心）

### 6.1 第一层 · 商业模式判断

- **一句话判断**（建档最低要求）：这家公司靠什么赚钱、你怎么看。
- **好处（pros）**：多条自由文本，商业模式的优势。
- **坏处（cons）**：多条自由文本，风险与短板。
- 以"好处 / 坏处"两栏对照呈现。

### 6.2 第二层 · 护城河评估

- 按 **5 种经典护城河类型**逐条判断（三态：**具备 / 不具备 / 存疑** + 备注/论据）：
  1. **无形资产**（品牌、专利、牌照、特许经营）
  2. **转换成本**（用户更换的代价高）
  3. **网络效应**（用户越多越有价值）
  4. **成本优势**（可持续的低成本）
  5. **有效规模 / 利基**（特定市场容不下第二个玩家）
- **护城河宽窄**：宽 / 窄 / 无（用户自评）。
- **趋势**：变宽 / 稳定 / 变窄（护城河是在加深还是被侵蚀）。
- **备注/论据**：支撑判断的理由。
- **5 种类型为默认模版，允许增删/自定义**（与管理层评估同一套模版机制）。
- 呈现：类型清单卡片 + 宽窄/趋势标记；可折叠。

### 6.3 第三层 · 管理层评估

- 列出该公司**主要管理层成员**（姓名/职务），**可从体检卡 `getLeaders` 自动带出**，也可手动增删。
- 对**每位成员**，按 **6 项默认标准**逐条判断（三态：**符合 / 不符合 / 存疑** + 备注）：
  1. **怎么花钱**（资本配置是否理性）
  2. **是否和小股东站在一起**
  3. **诚信**
  4. **说话能不能被验证**（言行是否一致、可回溯）
  5. **如何对待员工、客户、供应商**
  6. **看长期结果**（是否着眼长期而非短期）
- **6 项为默认模版，允许增删/自定义**。
- 呈现：每位成员一张卡片，6 项以清单/勾选态展示；可折叠。

### 6.4 第四层 · 自定义财务指标

- 用户挑选/添加自己关注的财务指标：
  - **内置指标（builtin）**：复用现有体系（PE/PE 分位、ROE、毛利率、资产负债率、股息率、扣非净利润、自由现金流等），挑选即用、可看迷你趋势（复用估值图表组件）。
  - **自定义（custom）**：自由填写指标名 + 自己的目标/判断（如"ROE 要长期 >15%"）。
- **预留监控挂钩**（`monitorLink`）：将来把指标挂到监控（L4），指标变化时提醒用户回看底稿。

### 6.5 第五层 · 买卖理由对比

- **买入理由**：多条，建仓/持有中记录（可分类：估值/成长/分红/护城河/管理层/催化…）。
- **卖出理由**：想卖时记录。
- **对比视图**：买入理由 与 卖出理由 并排呈现，看清"当初为什么买、现在为什么想卖"。
- **卖出前对质**：点「打算卖出」→ 列出所有 `active` 买入理由，逐条确认"仍成立/已不成立"；有理由破裂则引导记录卖出理由；全部成立则中立提示"确定要卖吗"，不阻拦。每次对质生成决策日志。

---

## 7. 数据模型（字段结构）

> 命名示意，最终以工程约定为准。MVP 可先存 `localStorage`（仿 `myDashboard`），登录后同步服务端（L2）。

```ts
// 持仓
interface Holding {
  id: string;
  code: string; displayCode: string; name: string | null; market: string;
  cost?: number; quantity?: number; buyDate?: string;   // 均可选
  status: 'holding' | 'closed';
  createdAt: number; updatedAt: number;
}

// 第一层：商业模式判断
interface BusinessModel {
  holdingId: string;
  summary?: string;      // 一句话判断（建档最低要求）
  pros: string[];        // 好处
  cons: string[];        // 坏处
  updatedAt: number;
}

// 第二层：护城河评估
interface MoatAssessment {
  holdingId: string;
  types: MoatTypeVerdict[];                 // 5 种经典类型
  width?: 'wide' | 'narrow' | 'none';       // 宽窄
  trend?: 'widening' | 'stable' | 'narrowing';  // 趋势
  note?: string;
  updatedAt: number;
}
interface MoatTypeVerdict {
  typeId: string;                           // 关联 MoatType
  verdict: 'yes' | 'no' | 'unsure';         // 三态：具备/不具备/存疑
  note?: string;
}
interface MoatType {                        // 默认5种，可增删自定义
  id: string; label: string; isDefault: boolean; order: number;
}
// 默认5种：intangibles(无形资产) / switching(转换成本) / network(网络效应)
//          / cost(成本优势) / scale(有效规模)

// 第三层：管理层评估
interface ManagementMember {
  id: string; holdingId: string;
  name: string; title?: string;
  source: 'auto' | 'manual';                // auto=从 getLeaders 带出
  note?: string;
  assessments: ManagementAssessment[];
}
interface ManagementAssessment {
  criterionId: string;
  verdict: 'yes' | 'no' | 'unsure';         // 三态
  note?: string;
}
interface ManagementCriterion {             // 默认6条，可增删自定义
  id: string; label: string; isDefault: boolean; order: number;
}
// 默认6条：capital / minorityAlign / integrity / verifiable / stakeholders / longterm

// 第四层：自定义财务指标
interface WatchedMetric {
  id: string; holdingId: string;
  source: 'builtin' | 'custom';
  metricKey?: string;     // builtin 时复用现有 MetricKey/估值指标
  label: string;
  targetNote?: string;    // 用户目标/判断，如"ROE>15%"
  monitorLink?: string;   // 预留：挂到监控(L4)
  order: number;
}

// 第五层：买卖理由 + 对质
interface HoldingReason {
  id: string; holdingId: string;
  type: 'buy' | 'sell';
  text: string;
  category?: 'valuation'|'growth'|'dividend'|'moat'|'management'|'catalyst'|'other';
  status: 'active' | 'broken';
  brokenNote?: string;
  createdAt: number; updatedAt: number;
}
interface SellReviewLog {
  id: string; holdingId: string; createdAt: number;
  checks: Array<{ reasonId: string; text: string; stillHolds: boolean }>;
  sellReason?: string;
  decision: 'sold' | 'keep' | 'thinking';
}
```

---

## 8. 功能需求

- **持仓列表**：搜索（`StockAutocomplete`）添加持仓；成本/数量/日期可选；展示底稿完成度与理由破裂标记；支持归档（closed 保留底稿）。
- **商业模式**：一句话判断 + 好处/坏处增删。
- **护城河**：5 种默认类型三态评估 + 宽窄/趋势 + 备注；类型可增删自定义。
- **管理层**：自动带出成员 → 逐人 6 项三态评估 + 备注；标准可增删。
- **财务指标**：从内置指标挑选（含迷你趋势）或自定义；预留监控挂钩。
- **买卖理由**：买入/卖出理由增删、分类、状态；对比视图；卖出前对质 + 决策日志。
- **复盘**：查看护城河/管理层/理由的变更历史与历次对质记录。

---

## 9. 交互（文字线框）

### 9.1 持仓详情页布局

```
┌───────────────────────────────────────┐
│  贵州茅台 600519            [归档] [删除]   │
├───────────────────────────────────────┤
│ ① 商业模式判断                             │
│   一句话：高端白酒，品牌+渠道双护城河           │
│   好处：品牌力 / 定价权 / 现金流               │
│   坏处：政策依赖 / 增速放缓                    │
├───────────────────────────────────────┤
│ ② 护城河评估          宽窄:[宽]  趋势:[稳定]    │
│   无形资产(品牌/专利)  [具备][不具备][存疑]      │
│   转换成本           [具备][不具备][存疑]      │
│   网络效应 / 成本优势 / 有效规模  …            │
│   [+ 自定义护城河类型]                        │
├───────────────────────────────────────┤
│ ③ 管理层评估（自动带出，逐条判断）              │
│   张三 · 董事长                             │
│    怎么花钱          [符合][不符合][存疑]       │
│    和小股东站一起     [符合][不符合][存疑]       │
│    诚信/可验证/待人/看长期  …                 │
│   [+ 添加成员]  [+ 自定义标准]                │
├───────────────────────────────────────┤
│ ④ 自定义财务指标                            │
│   ROE（内置·迷你趋势）  目标：长期>15%          │
│   [+ 从指标库添加]  [+ 自定义]                │
├───────────────────────────────────────┤
│ ⑤ 买卖理由对比          [打算卖出→对质]        │
│   买入理由            │  卖出理由             │
│   · 护城河稳固        │  （想卖时填写）         │
│   · 估值低位          │                      │
└───────────────────────────────────────┘
   本工具仅记录你自己的判断，不构成投资建议
```

### 9.2 卖出对质清单（见第 6.5）

点「打算卖出」→ 列出全部 active 买入理由逐条确认 → 有破裂则记卖出理由；全成立则中立提示、不阻拦 → 生成决策日志。

---

## 10. 与其他功能联动

- **持仓/自选**：复用现有持仓数据，叠加底稿层。
- **体检卡（L3）**：管理层名单从 `getLeaders` 自动带出；财务指标复用估值指标与图表；事件有大事时提示"影响你的底稿吗？"。
- **模版（L1）**：护城河类型、管理层标准都是"评估模版"，与投资人视角模版同一套模版思路。
- **监控（L4）**：底稿里的可量化指标挂监控，变动时提醒回看（进阶杀手锏）。

---

## 11. 存储与同步

- **MVP**：`localStorage` 本地，无需账号即可用。
- **完整价值**：跨设备需 L2 云端；持仓成本/底稿属**敏感个人数据**，云端需加密、可删除、注销即清除。

---

## 12. 合规与用户健康

- 纯记录**用户自己的**判断，不给买卖建议；全程中立措辞。
- LLM 自动带出的管理层信息标注"AI 整理，仅供参考"，用户判断为准。
- 不诱导交易，也不美化死扛；保留"不构成投资建议"提示。
- 敏感财务数据严格隔离加密（随 L2）。

---

## 13. 分期交付

| 子阶段 | 内容 | 依赖 |
|------|------|------|
| **v1（MVP）** | 持仓列表 + 详情页：商业模式（好处/坏处）+ 买卖理由对比 + 卖出对质 + 决策日志，本地存储 | 无 |
| **v1.1** | 护城河评估（5 类默认三态 + 宽窄/趋势 + 可增删） | 无 |
| **v1.2** | 管理层评估（6 项默认标准三态）；成员从 `getLeaders` 自动带出 | 体检卡管理层数据 |
| **v1.3** | 自定义财务指标（内置挑选 + 自定义 + 迷你趋势） | 估值指标 |
| **v1.4** | 云端同步（跨设备、敏感数据合规） | L2 |
| v2 | 指标挂监控 + 理由/标准动摇提醒；与事件日历联动 | L4 |

---

## 14. 非目标（Out of Scope）

- 不做真实下单/交易，不拦截交易。
- 不做买卖建议、目标价、评分。
- MVP 不自动判断理由/护城河/管理层是否成立（手动自查）。
- MVP 不做券商持仓自动同步（手动添加）。
- 不强制填写：任何板块都可留空。

---

## 15. 验收标准

- 能添加持仓并只填"一句话商业模式判断"即完成建档（渐进式可用）。
- 详情页五层均可正常录入、编辑、折叠、存草稿。
- 护城河可按 5 类做三态评估并标记宽窄/趋势；类型可增删。
- 管理层成员可自动带出并逐人做 6 项三态评估；标准可增删。
- 财务指标可从内置库挑选（带迷你趋势）或自定义。
- 买卖理由对比可用；卖出对质列出理由、生成决策日志；全程中立、不阻拦。
- 归档保留底稿；全程无"建议/该不该"措辞；免责到位。

---

## 16. 待确认问题

1. **增强现有 `PortfolioPage`** 还是**新增"持仓底稿"页**？（倾向增强现有，复用持仓数据）
2. 现有持仓体系是否已存成本/数量，可否直接复用为 `Holding`？
3. 护城河 5 类、管理层 6 项标准的最终文案与顺序；默认展开还是折叠？
4. 财务指标"内置库"暴露哪些（先给现有 8 类指标？）。
5. 管理层自动带出依赖 `getLeaders`（LLM），若不稳定是否降级为手动添加？
6. 敏感财务数据存储与加密策略，随 L2 一起定。

---

*本功能仅记录用户自己的投资判断，不构成投资建议。*

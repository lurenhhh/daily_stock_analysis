# 功能文档 03 · 个股事实体检卡 + 事件日历

> 版本：v0.2（新增「页面与信息架构」，体检卡定位为个股总览页）
> 日期：2026-08-25
> 上游依据：《产品规划-指标平台》Layer 3（客观信息聚合）
> 开发层级：**Layer 3**，大量复用估值 API，**零合规风险**
> 关联：复用估值页与财报查询；是「AI 摘要」（L5）与「监控推送」（L4）的数据地基

---

## 1. 功能概述

本层含两个相关功能：

- **个股事实体检卡**：输入公司 → **一页看清这家公司的客观事实**（财务、估值分位、股东结构、分红回购、管理层、限售解禁等），全部是硬数据、不加观点。定位为**个股总览页**（见第 3 节）。
- **事件日历**：把公司的**客观日程**（财报披露、除权除息、解禁、股东大会、回购进展）按时间线呈现，并可针对自选股汇总，帮用户"不错过重要事件"。

两者共享同一批客观数据源，放在一起做。

---

## 2. 背景与目标

### 2.1 为什么做

- **命中核心痛点**：散户查一家公司要在十几个地方翻（财报、股东、分红、解禁…）。体检卡把这些**聚合成一页**，直接解决"信息分散、看了忘"。
- **零合规风险**：全是客观事实与公开日程，不评分、不建议、不荐股，是全产品最安全的一类功能。
- **是地基**：体检卡是「AI 摘要」的输入源；事件日历是「监控推送」（L4）里**最安全的推送内容形态**（推送"事实要发生"，而非"你该买卖"）。

### 2.2 与估值页的分工（避免重复）

- **体检卡 = 广度**：一页速览一家公司的全貌，每个板块精简呈现，点进去链接到深度页。
- **估值页 = 深度**：估值分位、DCF、分部收入等细节分析。
- 体检卡**复用估值页的图表组件**，但以"缩略卡片 + 跳转详情"形式呈现，不重复造轮子、也不喧宾夺主。

---

## 3. 页面与信息架构（本次重点）

### 3.1 现状与问题

产品已在累积一批"搜索一只股票 → 看这只股票某方面"的页面：`估值分析`、`财报查询`（功能02），本层再加体检卡与日历。若每个都做成并列的顶部导航项，**导航会越来越臃肿**——恰恰是我们最初要摆脱的"同花顺式堆砌"。

### 3.2 目标架构：个股详情页（Tab 收纳）

长期方向是一个**「个股详情页」**，用 Tab 收纳同一只股票的各视图：

```
个股详情页（搜一次股票，Tab 间切换）
  ├─ 总览（体检卡）   ← 默认落地
  ├─ 估值            ← 现有估值页
  ├─ 财报            ← 功能02 财报查询
  └─ （未来：公告 / AI 摘要 …）
```

好处：搜一次股票就能看全，不用在多个页面间反复搜同一只票；导航保持精简，真正区别于同花顺。

### 3.3 落地策略：增量演进，不一次性重构

方向定"个股详情页"，但**分步走过去**，不拖慢上线：

- **本期**：体检卡作为**个股总览页**（新页面），承担未来 Tab 容器的角色；估值页、财报页暂以**跳转/链接**方式从体检卡各板块进入。
- **后续**：逐步把估值、财报收敛为体检卡页内的 Tab，形成真正的个股详情页。
- 因为体检卡各板块本就"精简+看详情跳转"，这个演进对现有估值页/财报页**改动可控**。

### 3.4 本层新增页面清单

| 页面 | 说明 | 是否新页面 |
|------|------|:---:|
| 个股总览页（体检卡） | 搜索股票 → 一页看全客观事实，含"未来大事"板块 | ✅ 新增 1 个 |
| 事件日历·全局视图 | 自选股所有事件按时间线汇总 | ✅ 新增 1 个（或挂在自选/看板区） |
| 事件日历·个股视图 | 这只票近期大事 | ❌ 不是新页面，是体检卡里的一个板块 |

> 净新增：**1 个体检卡（个股总览）页 + 1 个全局日历页**；个股日历是体检卡的板块。

---

## 4. 目标用户与场景

- **价值投资/长线用户**：决策前想快速把一家公司的客观底子摸一遍。
- **持仓用户**：想知道手里的票近期有什么大事（解禁、财报、除权）。
- 场景：模版看板发现一只票 → 打开体检卡（个股总览）三分钟看全客观事实 → 想深入就切/跳估值、财报。

---

## 5. 功能一：个股事实体检卡（个股总览页）

### 5.1 定位

一页、卡片式、客观事实速览，作为**个股总览页**。搜索一家公司后，纵向排布若干板块，每个板块是"事实 + 迷你图/表 + 看详情链接"。

### 5.2 板块设计

| 板块 | 内容（客观事实） | 数据来源 |
|------|----------------|---------|
| 公司概览 | 名称/代码/市场、所属行业、上市日期、当前市值 | 复用现有 stocks/valuation |
| 估值分位 | 当前 PE 及历史分位（高/合理/低）、迷你 PE 曲线 | **复用** `valuationApi.getPeHistory` |
| 十年财务概览 | 营收、总市值、扣非净利润、自由现金流 迷你趋势 | **复用** `getFundamentals` / `getMetrics` |
| 盈利质量 | ROE、毛利率、资产负债率、股息率（近年） | **复用** `getMetrics` |
| 分红与回购 | 历年分红、股息率、回购记录 | 新增（AkShare 分红送配/回购） |
| 股东结构 | 十大股东、机构持股比例、股东户数变化 | 新增（AkShare 十大股东/股东户数） |
| 管理层 | 主要领导人简介（标注"AI 整理"） | **复用** `getLeaders`（LLM，见 5.4） |
| 大事时间轴 | 公司重要里程碑 | **复用** `getMilestones`（LLM，见 5.4） |
| 分部收入 | 主营构成 | **复用** `getSegmentRevenue` |
| 限售解禁 | 未来解禁时间表与规模 | 新增（AkShare 限售解禁） |
| 未来大事 | 下次财报、除权、股东大会等（见功能二·个股视图） | 事件日历数据 |

> 大量板块**直接复用**你估值页已有的 API 与图表，新增数据主要是：分红回购、股东结构、限售解禁、事件日程。

### 5.3 交互

- 顶部 `StockAutocomplete` 搜索；下方卡片式板块纵向排列。
- 每个板块：精简呈现 + 「看详情」链接（跳估值页/财报页对应位置；未来收敛为 Tab 切换）。
- 板块**懒加载**：可见时再请求，避免一次打满所有接口（估值页部分接口较慢）。
- 每个板块标注**数据来源**。
- （可选增强）「导出/分享体检卡」——利于传播，后续做。

### 5.4 关于 LLM 生成板块（管理层/里程碑）的合规

- 体检卡以**硬客观数据**（财务/股东/分红/解禁）为主。
- `getLeaders` / `getMilestones` 是 LLM 生成，须**显著标注"AI 整理，仅供参考"**，且保持客观陈述（生平/职务/公开事件），不做评价。
- 若担心幻觉风险，MVP 可先把这两个板块设为**可选/折叠**，稳妥后再默认展开。

---

## 6. 功能二：事件日历

### 6.1 定位

呈现公司的**客观日程**，两种视图：

- **个股视图**：体检卡里的"未来大事"板块——这家公司近期/未来的事件（**不是独立页面**）。
- **全局视图**：把**自选股**的所有事件按时间线汇总，一眼看清"这周/这月我关注的票有什么大事"（**独立页面**）。

### 6.2 事件类型（全部客观）

| 事件 | 说明 | 数据来源 |
|------|------|---------|
| 财报披露 | 预约披露日期（A股 cninfo 有预约披露） | 新增（AkShare 财报披露计划） |
| 除权除息 | 分红派息、除权日 | 新增（AkShare 分红送配） |
| 限售解禁 | 解禁日期与规模 | 新增（AkShare 限售解禁） |
| 股东大会 | 召开日期 | 新增（AkShare/公告） |
| 回购进展 | 回购公告/进展 | 新增（AkShare/公告） |

### 6.3 与自选股

- 全局视图基于**自选股列表**（复用现有 stock pool / portfolio，无需新建）。
- 未登录时用本地自选；登录后用账号自选（L2 之后）。

### 6.4 与监控推送（L4）的边界 —— 重要

- **本层只做"展示"**：日历页面呈现事件，不主动推送。
- **推送提醒属 L4**：把"某自选股 3 天后解禁/明天披露财报"做成通知，是 L4 监控推送的内容。
- 但事件日历**天然是 L4 最安全的推送素材**（推送客观事实，不涉及买卖），本层把数据与结构准备好，L4 直接复用。

---

## 7. 数据源方案

- **A股（优先）**：以 **AkShare** 为主，覆盖十大股东、股东户数、分红送配、限售解禁、财报披露计划等（**具体接口名以 AkShare 当前版本为准**），复用现有 `data_provider` 集成，**免费**。
- **港股/美股**：覆盖度较弱，按第 11 节分期，先做 A股，其他市场给明确空态。
- **缓存**：这类数据变动慢（季度级），**缓存 TTL 可较长**（如 12–24h，财报季适当缩短）；复用现有存储/内存缓存，不上 Redis。
- **限流降级**：免费源限流 → 缓存 + 板块级降级（某板块拿不到数据只灰掉该板块，不影响整页）。

---

## 8. 与现有功能的关系

- **复用**：`StockAutocomplete`、估值 `valuationApi` 全套、`charts` 图表组件、`Card`/`PageHeader`/`EmptyState`/`ApiErrorAlert`、自选股 store。
- **联动**：体检卡各板块「看详情」→ 估值页 / 财报查询页（功能 02）/ 模版看板（未来收敛为 Tab）。
- **供源**：体检卡是 AI 摘要（L5）输入；事件日历是监控推送（L4）内容。
- **不进看板**：体检卡是页面视图，不作为 `DashboardItem`（但可提供"把某指标加入看板"的入口，复用现有 `AddToDashboardButton`）。

---

## 9. 合规设计

- 全部为**客观事实与公开日程**，不评分、不建议、不预测、不荐股。
- LLM 生成板块（管理层/里程碑）标注"AI 整理，仅供参考"并保持客观。
- 每板块标注数据来源；页面保留「不构成投资建议」免责。

---

## 10. 技术可行性与成本

- **数据成本 ≈ ¥0**：A股走 AkShare，复用现有集成；新增的分红/股东/解禁/日程接口都是免费源。
- **主要工作量**：新增几个数据抓取 + 体检卡页面（板块聚合、懒加载）+ 事件日历（个股视图 + 全局视图）。估值相关板块几乎零成本（复用）。
- **账号依赖**：体检卡（个股）**不依赖账号**；事件日历全局视图依赖"自选股列表"，未登录用本地、登录后用账号（L2 之后更完整）。

---

## 11. 分期交付

| 子阶段 | 内容 |
|------|------|
| **v1** | A股体检卡（个股总览页）：复用估值 API 的板块先上（概览/估值分位/财务/盈利质量/分部收入）+ 新增分红回购、股东结构、限售解禁 |
| **v1.1** | 管理层/里程碑板块（LLM，标注 AI 整理，默认可折叠） |
| **v1.2** | 事件日历·个股视图（体检卡"未来大事"板块） |
| **v1.3** | 事件日历·全局视图（自选股汇总时间线，独立页面） |
| v2 | 收敛为个股详情页 Tab（估值/财报并入）；港股/美股覆盖；体检卡导出/分享 |
| 衔接 L4 | 事件日历数据供监控推送复用（在 L4 做提醒） |
| 衔接 L5 | 体检卡供 AI 摘要复用 |

---

## 12. 非目标（Out of Scope）

- 不做任何评分、评级、买卖建议、目标价、预测。
- 本层不做事件的**主动推送**（属 L4）。
- v1 不做港股/美股（数据源覆盖后再说）。
- v1 不做体检卡导出/分享（后续增强）。
- v1 不强制重构估值/财报为 Tab（先用跳转，后续收敛）。

---

## 13. 验收标准

- 输入一家 A股公司，体检卡（个股总览页）能一页呈现各板块客观事实，板块懒加载、某板块失败不拖垮整页。
- 复用估值 API 的板块与估值页数据一致。
- 事件日历个股视图能列出该公司未来的财报/除权/解禁等日程。
- 全局视图能按自选股汇总事件并按时间排序。
- 所有板块标注来源；LLM 板块标注"AI 整理"；免责到位。
- 体检卡（个股）匿名可用。

---

## 14. 待确认问题

1. 是否认可"体检卡作为个股总览页、未来收敛为个股详情页 Tab"的信息架构方向？
2. 体检卡板块的**默认顺序与折叠策略**（哪些默认展开、哪些折叠）？
3. LLM 板块（管理层/里程碑）v1 是否上，还是等硬数据板块稳定后再加？
4. 事件日历全局视图未登录时，自选股取本地还是引导登录？
5. 新增数据（分红/股东/解禁/日程）的 AkShare 接口可用性与稳定性，需预研确认。

---

*本功能为客观信息聚合，仅呈现公开事实与日程，不构成投资建议。*

# 功能文档 01 · 投资人视角模版（Investment Lens Templates）

> 版本：v0.1（初稿）
> 日期：2026-08-25
> 状态：待评审
> 上游依据：《产品规划-指标平台》v0.1
> 开发层级：**Layer 1（第一层，最先开发）**

---

## 1. 文档说明与分层开发总览

本功能文档隶属于产品规划中的「支柱二：投资人视角模版」。整个产品按**由内到外、由轻到重**的顺序分层开发，本文档只覆盖 **Layer 1**。

### 1.1 分层开发路线（Feature Layers）

| 层级 | 功能 | 依赖 | 说明 |
|------|------|------|------|
| **L1** | **投资人视角模版（本文档）** | 仅复用现有估值 API + 我的看板 | 无需账号、无需新后端，成本最低、验证价值最高 |
| L2 | 账号体系 + 云端同步 | 后端 + 数据库 | 让看板/模版跨设备、可商业化的基础设施 |
| L3 | 个股事实体检卡 + 事件日历 | 部分复用估值 API | 客观信息聚合，零合规风险 |
| L4 | 监控与异动推送 | L2 + 推送通道 | 纯工具的留存心脏 |
| L5 | AI 财报/公告摘要 | LLM + 数据源 | 获客亮点 |
| L6 | 投资笔记 / 第二大脑 | L2 | 留存护城河 |

### 1.2 为什么 Layer 1 是它

1. **复用已有资产**：你已完成的 `估值分析` 页（8 类指标镜头）+ `我的看板`（`DashboardItem` 结构、拖拽、增删）正好是模版的落地容器。
2. **零基础设施依赖**：首版模版可作为**前端静态配置**硬编码，应用后写入现有 `localStorage` 看板，**不需要账号、不需要新增后端接口**。
3. **一箭四雕**：同时解决冷启动、获客传播、用户教育、通往自定义（见规划文档）。
4. **风险可控**：只要守住「镜头非选股器」的边界（见第 7 节），合规风险低。

---

## 2. 功能目标

- **主目标**：消除「我的看板」的空画布冷启动——新用户一键套用一个投资人视角模版，立刻得到一套有观点、有解释的指标看板。
- **次目标**：通过「有解释的模版」传递方法论，教育用户「该看什么」，并成为可对外传播的内容钩子。
- **不做**：不推荐股票、不筛选/排名股票、不给买卖信号（见第 7 节合规设计）。

### 成功度量（建议）

- 新用户首个看板中，**由模版生成的占比**（目标：> 60%）。
- **模版应用率**：访问模版库并成功应用的用户占比。
- 应用模版后的**看板留存/回访**（次周仍保留 ≥1 个模版看板的用户占比）。
- 模版详情页「方法论说明」的展开/阅读率。

---

## 3. 核心概念与数据模型

### 3.1 模版 = 与股票无关的「镜头清单」

现有 `DashboardItem` 是**绑定到具体股票**的（含 `code`/`displayCode`/`name`）。模版则是**与股票无关**的、只描述「要看哪些指标、怎么看」的清单。用户选定股票后，模版被「实例化」成一组 `DashboardItem`。

这一层抽象正是合规的关键：**模版是作用在「用户已经选好的股票」上的观察镜头，而不是一个帮用户挑股票的筛选器。**

### 3.2 模版镜头项（LensSpec）—— 概念结构

> 以下为概念性字段定义，用于表达设计意图，**具体实现由开发决定**。

```
LensTemplate {
  id: string                 // 模版唯一标识，如 "moat"
  name: string               // 展示名（方法论命名，见第 7.3 节），如 "护城河镜头"
  tagline: string            // 一句话简介
  philosophy: string         // 方法论说明（面向用户的教育文案）
  reference?: string         // 参考的投资理念来源说明（放说明，不做商品名）
  lenses: LensSpec[]         // 有序的镜头清单
  defaultYears: number       // 默认时间跨度（10 / 20）
}

LensSpec {
  chart: 'pe' | 'fund' | MetricKey   // 复用现有 DashboardChartKind
  metric?: 'pe_ttm' | 'pe'           // 仅 chart='pe' 时使用
  years?: number                     // 缺省用模版 defaultYears
  note: string                       // 该镜头在本模版里「为什么看、怎么看」的说明
  referenceHint?: string             // 客观参考提示（非买卖建议，见第 7 节）
}
```

其中 `chart` / `metric` / `MetricKey` **完全复用你现有的定义**（`utils/myDashboard.ts` 与 `api/valuation.ts`）：

- `pe`（配 `metric`：`pe_ttm` / `pe`）——历史 PE 与估值分位
- `fund`——总营收 + 总市值
- `grossMargin` 毛利率 · `roe` ROE · `freeCashFlow` 自由现金流 · `dividendYield` 股息率 · `debtRatio` 资产负债率 · `deductedNetProfit` 扣非净利润

### 3.3 应用模版 = 批量生成 DashboardItem

应用流程在数据层面等价于：对模版的每个 `LensSpec`，用用户选定的股票 `code/displayCode/name` 生成一个 `DashboardItem`，调用现有 `addDashboardItem`（已自带去重逻辑）。**无需新增存储机制。**

---

## 4. 用户故事

- 作为一个**刚进来的新用户**，我打开「我的看板」看到的不是空白，而是几个可选的投资人视角模版，我能点一个、选一只我关心的股票，立刻得到一套看板。
- 作为一个**不知道该看什么指标的用户**，我想通过模版的说明理解「这个视角关注哪些指标、为什么」，从而学会自己看。
- 作为一个**有想法的进阶用户**，我想在模版生成的看板基础上，删掉不需要的、加上我自己的指标，微调成我自己的看板。
- 作为一个**看多只股票的用户**，我想把同一个模版分别套到几只股票上做观察（v1 可单只，多只对比列入后续）。

---

## 5. 功能需求

### 5.1 模版库入口

- 在「我的看板」**空状态**处，用模版库替代当前的 `EmptyState`：展示官方模版卡片（名称、tagline、包含的镜头数量、缩略指标标签）。
- 在「我的看板」**非空状态**也提供入口（如页头「从模版添加」按钮），允许随时再套用模版。
- 每个模版卡片提供两个动作：**查看详情** / **应用模版**。

### 5.2 模版详情

- 展示：模版名、tagline、**方法论说明（philosophy）**、参考理念来源（reference）、镜头清单（每项含 `note` 说明与 `referenceHint` 客观提示）。
- 详情页可直接触发「应用模版」。
- 方法论说明是本功能的**内容护城河**，需认真撰写（见第 6 节文案），不是占位文字。

### 5.3 应用模版到个股（核心交互）

1. 用户点击「应用模版」。
2. 弹出/进入**选股步骤**：复用现有 `StockAutocomplete` 组件让用户输入/选择一只股票。
3. 确认后，系统按模版 `lenses` 为该股票批量生成 `DashboardItem`（复用 `addDashboardItem`），已存在的镜头自动去重。
4. 跳转/停留在「我的看板」，展示新生成的一组看板；沿用现有拖拽网格与增删能力。
5. 生成的每个看板项，保留现有「移除」能力；用户可自由微调（删项）。

> v1 交互可最简：模版 → 选一只股票 → 生成。不引入多选股票、不引入模版内参数编辑。

### 5.4 与现有「我的看板」的关系

- 模版生成的项与用户手动「加入看板」的项**同构**（都是 `DashboardItem`），在同一网格里共存，无需区分来源。
- （可选增强）为 `DashboardItem` 记录来源模版标识，便于后续「按模版分组显示」或统计度量；此为增强项，非 v1 必需。

### 5.5 空状态改造

- 当前 `MyDashboardPage` 的空状态是一个静态 `EmptyState`。本功能将其升级为**模版库**，这是治冷启动的核心落点。

---

## 6. 首批官方模版定义（v1 建议 3 个）

> 以下镜头均从你现有的 8 类指标中组合，**无需新增数据能力**。阈值/提示一律为**客观财务常识的陈述性参考**，不构成买卖建议（措辞规范见第 7 节）。文案为初稿，可再打磨。

### 模版 A：护城河镜头（moat）

- **tagline**：看一家公司是不是「好生意」。
- **方法论说明（philosophy）**：好生意的共同特征，是长期、稳定的高资本回报，健康的盈利质量，以及能持续产生现金。本镜头把这些客观指标放在一起，帮你观察一家公司的「生意质地」，而不是判断它此刻该不该买。
- **镜头清单**：
  - `roe`（ROE）—— note：长期 ROE 水平反映资本回报效率；参考提示：长期稳定在较高水平通常被视为盈利能力较强（客观陈述）。
  - `grossMargin`（毛利率）—— note：毛利率的高低与稳定性反映产品/服务的定价能力。
  - `freeCashFlow`（自由现金流）—— note：持续为正的自由现金流反映「赚到的是真钱」。
  - `fund`（营收 + 市值）—— note：观察营收长期成长与市值的关系。
  - `pe`（`pe_ttm`）—— note：在了解生意质地后，用估值分位观察当前定价处于历史什么位置。
- **defaultYears**：20

### 模版 B：逆向低估镜头（contrarian）

- **tagline**：观察那些「便宜」但基本面未必恶化的公司。
- **方法论说明**：逆向视角关注估值处于历史低位的公司，但便宜本身不是理由——还需确认盈利与财务是否稳健。本镜头把「估值分位」与「基本面健康度」并排呈现，供你自行判断，不筛选、不推荐任何标的。
- **镜头清单**：
  - `pe`（`pe_ttm`，years=20）—— note：观察当前估值处于历史 20 年的什么分位。
  - `deductedNetProfit`（扣非净利润）—— note：剔除非经常损益，看主业是否仍在真实盈利。
  - `debtRatio`（资产负债率）—— note：评估财务安全边际，避免「便宜有便宜的原因」。
  - `dividendYield`（股息率）—— note：低估叠加分红，是逆向视角常关注的组合。
- **defaultYears**：20

### 模版 C：稳健分红镜头（dividend）

- **tagline**：关注现金回报与财务稳健。
- **方法论说明**：偏好现金回报的观察视角，关注分红的持续性、现金流的支撑能力与财务的稳健程度。本镜头呈现相关客观指标，供你评估一家公司的「分红体质」。
- **镜头清单**：
  - `dividendYield`（股息率）—— note：当前股息率与其历史水平。
  - `freeCashFlow`（自由现金流）—— note：分红的可持续性取决于是否有现金支撑。
  - `debtRatio`（资产负债率）—— note：高负债可能侵蚀分红能力。
  - `roe`（ROE）—— note：长期资本回报的稳定性。
  - `pe`（`pe_ttm`）—— note：观察当前估值分位。
- **defaultYears**：20

> 后续可扩展：成长质量镜头、周期观察镜头等。首批控制在 3 个精品，重质不重量。

---

## 7. 合规设计（本功能红线）

### 7.1 镜头，而非选股器

- 模版**必须**由用户先选定股票后再作用于该股票。
- **严禁**出现「用模版筛选出一批股票并列表展示」的形态——那等同于荐股。
- 界面文案统一表述为「观察 / 查看 / 呈现」，避免「优选 / 推荐 / 精选 / 该买」。

### 7.2 参考提示的措辞

- 所有 `referenceHint` 只能是**客观财务常识的陈述句**（如「ROE 是净利润与净资产的比值」「毛利率反映定价能力」）。
- **禁止**出现择时或买卖导向的表述（如「低于 X 可买入」「适合建仓」）。
- 涉及数值参考时，加「历史/常识」限定与「不构成投资建议」提示。

### 7.3 命名规范

- 模版对外名用**方法论命名**（护城河镜头 / 逆向低估镜头 / 稳健分红镜头）。
- **不**用在世或已故投资人的真名做模版商品名或主打卖点；如需引用其理念，放在 `reference` 说明字段里，表述为「参考了 XX 的投资理念」。
- 全站保留统一的「本内容不构成投资建议」免责声明。

---

## 8. 技术落地建议（不涉及具体改码，仅界定范围）

- **模版数据来源（v1）**：官方模版作为**前端静态配置**内置，无需后端接口、无需数据库。
- **数据获取**：应用模版后各镜头的数据，**完全复用现有 `valuationApi`**（`getPeHistory` / `getFundamentals` / `getMetrics`），无需新增接口。
- **落地存储（v1）**：复用现有 `localStorage`（`dsa:myDashboard`）与 `addDashboardItem`。
- **组件复用**：选股复用 `StockAutocomplete`；看板渲染复用 `DraggableChartGrid` 与 `charts` 组件。
- **结论**：Layer 1 **不新增后端、不依赖账号**，主要是前端的模版配置 + 空状态改造 + 应用流程。

---

## 9. Layer 1 内部分期

| 子阶段 | 内容 | 依赖 |
|------|------|------|
| **1.0（MVP）** | 3 个官方静态模版 + 模版库空状态 + 应用到单只股票 + 复用 localStorage 看板 | 无新增 |
| 1.1 | 模版详情页方法论说明完善 + 度量埋点 | 无新增 |
| 1.2 | 应用到多只股票 / 同模版对比视图 | 无新增（前端） |
| 1.3 | 用户自定义模版（把当前看板存为「我的模版」） | 需 L2 账号/云端同步 |
| 未来 | UGC 模版市场（分享/发现他人模版） | L2 + 社区基建 |

> 1.0–1.2 均可在无账号、无后端的前提下完成；1.3 起需要 Layer 2 支撑。

---

## 10. 非目标（Out of Scope）

- 不做股票筛选器 / 排名 / 打分。
- 不做买卖信号、目标价、评级。
- v1 不做模版内指标的参数编辑（阈值、周期由模版预设，用户只能整体套用后再增删）。
- v1 不做账号与云端同步（属 Layer 2）。
- 不做模版历史回测（属谨慎后置项）。

---

## 11. 待确认问题

1. 首批 3 个模版的清单与命名是否确认？镜头组合与文案是否需要调整？
2. 应用模版的交互形态：弹窗选股 vs 独立页面步骤，倾向哪种？
3. 是否要在本期就给 `DashboardItem` 增加「来源模版」标识（利于后续分组与度量）？
4. 多只股票对比视图放本期（1.2）还是延后？
5. 免责声明与合规文案的最终措辞，是否需要法务/合规同学过一遍？

---

*本文档为 Layer 1 功能设计草稿。所有涉及投资的呈现均以「观察镜头、不构成投资建议」为前提。*

# 功能文档 · 持仓纪律（当前实现 / as-built）

> 版本：as-built v1.0
> 日期：2026-08-27
> 说明：本文档**反映截至今日代码里的实际实现**（非设计草案）。设计意图见《功能文档-04-持仓纪律》v0.3。
> 涉及代码：前端 `apps/dsa-web`（pages/holdings、components/holdings、utils/holdingDiscipline、contexts/UserAccountContext）；后端 `api/account`、`account_service`、`storage`。

---

## 1. 功能定位

给用户持有的每家公司建立一份**结构化投资底稿**（五层），并在卖出前用买入理由做**对质**，帮用户对抗情绪化交易。全程只记录用户自己的判断，不给买卖建议。

---

## 2. 页面、路由与入口

| 路由 | 页面 | 职责 |
|------|------|------|
| `/discipline` | `HoldingDisciplinePage` | 持仓列表页：添加/管理持仓，进入底稿 |
| `/discipline/:holdingId` | `HoldingDossierPage` | 底稿详情页：五层结构 |

- 导航入口：`SidebarNav` 中 key=`discipline`、图标 `ShieldCheck`，归在**「我的分析」**分组。
- 组件：`components/holdings/`（`HoldingCard`、`DossierLayers`、`SellConfrontationModal`）。

---

## 3. 持仓列表页（`/discipline`）

- 用 `StockAutocomplete` 搜索添加持仓；同一 code 且在持有中的自动去重（`addHolding`）。
- 列表以卡片（`HoldingCard`）展示各持仓；**有买入理由被标记为破裂（broken）时显示告警**（`AlertTriangle`）。
- 点击卡片进入该持仓的底稿详情页。

---

## 4. 底稿详情页（`/discipline/:holdingId`）· 五层

以可折叠 `Section`（带序号+标题）自上而下呈现，进入时按 `holdingId` 加载：

### ① 商业模式（BusinessModel）
- 一句话判断 `summary` + 好处 `pros[]` + 坏处 `cons[]`。

### ② 护城河（MoatAssessment）
- **5 类默认护城河类型**（label 走 i18n）：无形资产 `intangibles`、转换成本 `switching`、网络效应 `network`、成本优势 `cost`、有效规模 `scale`；**支持自定义类型** `customTypes[]`。
- 每类三态判断 `verdict: yes|no|unsure`（具备/不具备/存疑）+ 备注 `note`。
- 整体 **宽窄** `width: wide|narrow|none`、**趋势** `trend: widening|stable|narrowing`。

### ③ 管理层（ManagementMember）
- **支持从体检卡自动带出成员**：调用 `valuationApi.getLeaders(code,{name})` → 逐个 `addMember(..., 'auto')`；也可手动添加（`source: manual`）。
- **6 项默认标准**（label 走 i18n）：怎么花钱 `capital`、和小股东站一起 `minorityAlign`、诚信 `integrity`、可验证 `verifiable`、对待利益相关方 `stakeholders`、看长期 `longterm`；**支持自定义标准**（按 holdingId 存 `mgmtCriteria`）。
- 每位成员对每条标准三态判断 + 备注。

### ④ 自定义财务指标（WatchedMetric）
- **内置指标（builtin）**：复用 `valuationApi.getMetrics`（详情页拉一次，20 年），选中的指标显示**迷你趋势**；`metricKey` 复用现有指标体系。
- **自定义指标（custom）**：自填名称。
- 支持 `targetNote`（目标/判断）；预留 `monitorLink`（未来挂监控 L4）。

### ⑤ 买卖理由（HoldingReason）
- 理由分 `type: buy | sell`，可选分类 `category`（估值/成长/分红/护城河/管理层/催化/其他），状态 `status: active | broken`。
- 买入理由与卖出理由对比呈现。

### 顶部操作
- 归档 `Archive` / 恢复 `RotateCcw` / 删除 `Trash2`（归档=`status: closed`，保留底稿）。

---

## 5. 卖出对质流程（`SellConfrontationModal`）

在详情页触发"打算卖出"后：

1. 列出该持仓所有买入理由，用户逐条确认是否仍成立 → 生成 `checks[]`（`{reasonId, text, stillHolds}`）。
2. 填写卖出理由 `sellReason`（可空），选择决定 `decision: keep | sold | thinking`。
3. 提交后：
   - 写入一条 `SellReviewLog`（`addSellReviewLog`），作为决策日志留档。
   - 被判为不成立的理由，`setReasonStatus(id,'broken', 说明)` 标记为破裂（列表页据此告警）。
- 全程中立措辞，不阻拦卖出。

---

## 6. 数据模型（实际 · `utils/holdingDiscipline.ts`）

单一 `Store` 对象存储；三态统一为 `TriState = 'yes'|'no'|'unsure'`。

```ts
interface Store {
  holdings: Holding[];
  reasons: HoldingReason[];        // 买/卖理由
  logs: SellReviewLog[];           // 卖出对质日志
  business: Record<string, BusinessModel>;   // holdingId -> 商业模式
  moat: Record<string, MoatAssessment>;      // holdingId -> 护城河
  members: ManagementMember[];               // 含 holdingId
  metrics: WatchedMetric[];                  // 含 holdingId
  mgmtCriteria: Record<string, MoatCustomType[]>; // holdingId -> 自定义管理层标准
}

// Holding: code/displayCode/name/market/cost?/quantity?/buyDate?/status('holding'|'closed')
// BusinessModel: summary?/pros[]/cons[]
// MoatAssessment: width?/trend?/note?/verdicts(typeId->{verdict,note})/customTypes[]
// ManagementMember: name/title?/source('auto'|'manual')/note?/verdicts(criterionId->{verdict,note})
// WatchedMetric: source('builtin'|'custom')/metricKey?/label/targetNote?/monitorLink?/order
// HoldingReason: type('buy'|'sell')/text/category?/status('active'|'broken')/brokenNote?
// SellReviewLog: checks[{reasonId,text,stillHolds}]/sellReason?/decision('sold'|'keep'|'thinking')

const DEFAULT_MOAT_TYPES   = ['intangibles','switching','network','cost','scale'];
const DEFAULT_MGMT_CRITERIA= ['capital','minorityAlign','integrity','verifiable','stakeholders','longterm'];
```

- 默认清单的 label 走 i18n：`discipline.moat.type.<id>` / `discipline.mgmt.crit.<id>`。

---

## 7. 存储与同步（实际）

### 本地
- 全部存 `localStorage`，key `dsa:holdingDiscipline`；`loadStore/saveStore` 集中读写，`saveStore` 触发 `HOLDINGS_CHANGED_EVENT`。

### 云端（已接入）
- 后端表 `user_dossier`（`user_id` 主键、`data_json`、`updated_at`），`DossierService.get/save`（JSON blob）。
- 接口：`GET /api/v1/account/dossier` → `{data, updatedAt}`；`PUT /api/v1/account/dossier`（body `{data}`，last-write-wins）。
- 同步编排在 `contexts/UserAccountContext.tsx`：
  - **登录/查会话后**：拉服务端底稿；服务端有内容（`holdings` 非空）→ `replaceStoreFromCloud` 覆盖本地；服务端为空而本地有内容 → `putDossier` 迁移上去。
  - **变更上推**：监听 `HOLDINGS_CHANGED_EVENT`，登录态下去抖 1.5s 后 `putDossier(exportStore())`。
  - **登出**：`clearDiscipline()` 清本地（服务端已保留）。

---

## 8. 复用的现有能力

- `StockAutocomplete`（搜索添加持仓）。
- `valuationApi.getLeaders`（管理层自动带出）、`valuationApi.getMetrics`（内置指标迷你趋势）。
- `components/common`（`AppPage`/`Card`/`PageHeader`/`EmptyState`）。
- 账号体系与 `/account` 同步机制（与看板同步共用一套）。

---

## 9. 合规

- 纯记录用户自己的判断，不给买卖建议；卖出对质中立、不阻拦。
- 管理层信息经 `getLeaders`（LLM）自动带出，属"AI 整理"性质，用户判断为准。
- 持仓成本等属敏感个人数据（加密/注销清除随 L2 整体合规补齐，见下）。

---

## 10. 已知限制与待办

> 以下为当前实现的已知问题/欠缺，供后续排期（详见此前代码检查结论）。

1. **登出时的空回写风险**：`logout` 中 `clearDiscipline()` 会发变更事件，监听器可能尚未卸载 → schedule 一次 `putDossier(空)`；目前靠登出后 cookie 失效返回 401 兜底保住服务端数据，较脆弱。建议登出时先取消 pending 定时器/移除监听再清本地。
2. **登录后冗余回写**：`replaceStoreFromCloud` 会发事件 → 登录拉取后又把同样数据 PUT 回一次（不死循环，因 put 不写本地）。建议加 `isApplyingRemote` 抑制标志。
3. **判空口径不一致**：同步时"服务端为空"用 `holdings` 非空判断，本地内容用更宽的 `storeHasContent`；建议统一。
4. **同步冲突**：last-write-wins，无条目级合并（MVP 可接受）。
5. **敏感数据**：`user_dossier` 明文 JSON 存储，加密与账号注销清除随 L2 合规补齐。

---

## 11. 关联文档

- 《功能文档-04-持仓纪律》v0.3（设计意图与五层结构）。
- 《开发任务-持仓底稿云端同步》（本次云端同步的实现依据）。
- 《开发文档-L2-账号体系与云端同步》/《L2-lite》（账号与同步地基）。

---

*本功能仅记录用户自己的投资判断，不构成投资建议。*

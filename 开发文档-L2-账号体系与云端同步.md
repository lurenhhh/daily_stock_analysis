# 开发文档 · L2 账号体系 + 云端同步 + 数据库

> 版本：v0.1
> 日期：2026-08-25
> 上游：《产品规划-指标平台》Layer 2
> 适用工程：后端 `api/` + `src/`（FastAPI + SQLAlchemy）、前端 `apps/dsa-web`
> 定位：让看板 / 模版跨设备、为商业化打底的基础设施层

---

## 1. 目标与范围

- **多用户账号体系**：从"单管理员口令"升级为真正的多用户注册/登录，每个用户的数据相互隔离。
- **云端同步**：看板（`DashboardItem`）与自定义模版从 `localStorage` 迁移到服务端为准，跨设备一致。
- **商业化地基**：预留会员/权限（entitlements）模型，为后续 freemium 配额（看板数、监控数）打底。
- **不做**（超出本层）：支付/订单/微信支付对接、监控推送引擎、具体会员售卖页（各自独立层）。

---

## 2. 现状分析（升级前必须认清）

| 维度 | 现状 | 商业化要求 | 差距 |
|------|------|-----------|------|
| 认证 | 单管理员口令（`src.auth`：`is_auth_enabled`/`verify_session`/`COOKIE_NAME`），登录仅需一个密码，无用户身份 | 多用户注册/登录，手机号/微信 | **需重构为多用户** |
| 用户数据 | 看板存浏览器 `localStorage`（`dsa:myDashboard`） | 服务端为准、跨设备、可备份 | **需服务端化** |
| 数据隔离 | 无用户维度 | 所有用户数据按 `user_id` 隔离 | **需引入 user_id** |
| 数据库 | SQLAlchemy + `DatabaseManager`（默认 SQLite） | 生产多用户并发 | 建议切 PostgreSQL（SQLAlchemy 抽象，切换成本可控） |
| 已有可复用 | `get_db()` 依赖、service 分层、鉴权中间件、`AuthContext`/`authApi` | — | **在此之上扩展** |

> 核心判断：现有单管理员口令模式适合"自部署单人使用"，与"ToC 多租户"是两种形态。建议**保留自部署单用户模式**（默认用户），同时新增 **SaaS 多用户模式**，由配置开关切换，OSS 用户不受影响。

---

## 3. 架构决策

### 3.1 从"单管理员口令"到"多用户账号"

- 引入 `users` 表与用户身份；会话（session）从"是否登录"升级为"绑定到某个 `user_id`"。
- 扩展 `src.auth`：`verify_session()` 返回 `user_id`（或 `None`）；新增 `get_current_user` 依赖。
- **数据隔离原则**：所有用户私有数据表都带 `user_id` 外键，所有查询强制按 `current_user.id` 过滤（禁止跨用户访问）。
- **兼容模式**：
  - `AUTH_MODE=single`（默认，自部署）：沿用现有口令，内部映射到一个固定 `default_user`。
  - `AUTH_MODE=saas`（商业化）：启用注册/登录、多用户。

### 3.2 认证方式（面向中国 ToC）

优先级建议：

1. **手机号 + 短信验证码（主）**——中国 ToC 主流，手机号本身满足实名制要求。
2. **微信登录（快速跟进）**——ToC 获客关键，建议 M3 接入（OAuth2 网页授权 / 移动端）。
3. **邮箱 + 密码（次）**——海外/兜底。

**会话机制**：服务端不透明 token（opaque session token）存 DB/Redis，支持多设备并存与单独吊销。

- Web 端：`httpOnly` + `Secure` + `SameSite=Lax` Cookie（沿用现有 `COOKIE_NAME` 思路）。
- 未来移动端：同一 token 走 `Authorization: Bearer`。
- Token 记录设备信息与 `expires_at`，可在"登录设备管理"里逐个下线。

### 3.3 同步策略（服务端为准 + 本地缓存）

采用**服务端为权威源 + 本地缓存 + 乐观并发**：

- 服务端保存每个用户的看板与自定义模版的权威版本，带 `version`（单调递增）和 `updated_at`。
- 前端保留 `localStorage` 作为**离线缓存**与**未登录时的本地态**。
- 读：登录后 `GET` 拉取服务端版本，覆盖/合并本地缓存。
- 写：本地先乐观更新 → 异步 `PUT` 到服务端；请求带客户端已知 `version`。
- **冲突处理（MVP 简化）**：看板整体作为一个用户文档，服务端校验 `version`：
  - 版本匹配 → 接受，`version+1`。
  - 版本落后（别的设备已改）→ 返回 `409` + 最新版本，前端以**服务端为准**刷新（MVP 阶段 last-write-wins 足够；后续可做条目级合并）。
- 写操作**去抖/批量**（如 500ms 合并），避免拖拽/连续增删打爆接口。

### 3.4 未登录 → 登录的数据迁移

用户在未登录状态下已用模版生成过本地看板：登录/注册成功后，**一次性把本地 `localStorage` 看板导入服务端**（见 6.3），随后服务端为准，本地转为缓存。

---

## 4. 数据模型（数据库表）

> 命名用 snake_case，与现有后端一致；主键建议 `id`（UUID 或雪花/自增，按现有约定）。以下为逻辑 schema，字段类型示意。

### 4.1 用户与认证

```
users
  id            PK
  phone         varchar unique nullable   -- 手机号（主登录标识，saas 模式）
  email         varchar unique nullable
  nickname      varchar
  avatar_url    varchar nullable
  status        enum(active, disabled)    default active
  created_at    timestamptz
  updated_at    timestamptz

user_credentials                          -- 密码类凭证（可选，邮箱/密码用）
  id            PK
  user_id       FK -> users.id
  type          enum(password)            -- 预留 wechat 等
  secret_hash   varchar                   -- argon2/bcrypt 哈希，绝不存明文
  created_at    timestamptz

user_oauth                                -- 第三方登录绑定（微信等，M3）
  id            PK
  user_id       FK -> users.id
  provider      enum(wechat)
  open_id       varchar
  union_id      varchar nullable
  unique(provider, open_id)

sessions                                  -- 会话 token（多设备）
  id            PK
  user_id       FK -> users.id
  token_hash    varchar unique            -- 存哈希，不存原 token
  device_info   varchar nullable
  ip            varchar nullable
  created_at    timestamptz
  expires_at    timestamptz
  revoked_at    timestamptz nullable

sms_codes                                 -- 短信验证码（也可放 Redis，见 11）
  id            PK
  phone         varchar
  code_hash     varchar
  purpose       enum(login, bind)
  expires_at    timestamptz
  consumed_at   timestamptz nullable
  send_count    int                       -- 频控用
```

### 4.2 看板与模版（云端化 localStorage）

```
dashboards                                -- 每用户可有一个或多个看板（MVP 先一个）
  id            PK
  user_id       FK -> users.id
  name          varchar        default '我的看板'
  version       bigint         default 1  -- 乐观并发
  updated_at    timestamptz

dashboard_items                           -- 对应前端 DashboardItem
  id            PK
  dashboard_id  FK -> dashboards.id
  code          varchar                   -- 规范化股票代码
  display_code  varchar
  name          varchar nullable
  chart         varchar                   -- 'pe' | 'fund' | MetricKey
  metric        varchar nullable          -- pe_ttm | pe
  years         int
  source_template_id varchar nullable     -- 来源模版（埋点/分组，呼应 L1 后续项）
  sort_order    int                       -- 保留拖拽顺序
  created_at    timestamptz

user_templates                            -- 用户自定义模版（L1 子阶段 1.3 依赖本层）
  id            PK
  user_id       FK -> users.id
  name          varchar
  icon          varchar nullable
  philosophy    text nullable
  lenses_json   jsonb                     -- LensSpec[] 序列化
  version       bigint default 1
  created_at    timestamptz
  updated_at    timestamptz
```

### 4.3 会员与权限（商业化预留，本层只建模不做售卖）

```
user_entitlements                         -- 当前权益快照（免费/付费）
  user_id       PK/FK -> users.id
  plan          enum(free, pro)   default free
  valid_until   timestamptz nullable
  updated_at    timestamptz

-- 配额在代码里以 plan 映射常量表达（示例）：
--   free: { max_dashboard_items: 20, max_user_templates: 1, max_monitors: 3 }
--   pro : { max_dashboard_items: 200, max_user_templates: 50, max_monitors: 100 }
```

> 说明：`subscriptions`/订单/支付流水属独立"支付层"，本层不建。`user_entitlements` 作为权益读取的唯一入口，支付层未来只负责写它。

---

## 5. API 设计

所有新增接口挂在现有 `/api/v1` 下，沿用现有 router 聚合方式（`api/v1/router.py`）。用户私有接口统一依赖 `get_current_user`。

### 5.1 认证（扩展现有 `auth` router）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|:---:|
| POST | `/api/v1/auth/sms/send` | 发送短信验证码（含频控） | 否 |
| POST | `/api/v1/auth/register` | 手机号+验证码 注册/登录合一 | 否 |
| POST | `/api/v1/auth/login` | 手机号+验证码 或 邮箱+密码 | 否 |
| POST | `/api/v1/auth/logout` | 注销当前会话 | 是 |
| GET | `/api/v1/auth/status` | 返回登录态（扩展现有，加 user 概要） | 否 |
| GET | `/api/v1/me` | 当前用户资料 + 权益 | 是 |
| GET | `/api/v1/me/sessions` | 登录设备列表 | 是 |
| DELETE | `/api/v1/me/sessions/{id}` | 下线某设备 | 是 |
| GET | `/api/v1/auth/wechat/callback` | 微信登录回调（M3） | 否 |

请求/响应示例（注册/登录合一）：

```jsonc
// POST /api/v1/auth/sms/send
{ "phone": "138xxxx0000", "purpose": "login" }
// -> 200 { "ok": true, "cooldown": 60 }

// POST /api/v1/auth/login
{ "phone": "138xxxx0000", "code": "123456" }
// -> 200 Set-Cookie: <session>; { "user": { "id": "...", "nickname": "...", "plan": "free" } }
```

### 5.2 看板同步

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/me/dashboard` | 拉取当前用户看板（含 items、version） |
| PUT | `/api/v1/me/dashboard` | 全量覆盖保存（带 version 乐观并发） |
| POST | `/api/v1/me/dashboard/import` | 从本地导入（首次登录迁移，见 6.3） |

```jsonc
// GET /api/v1/me/dashboard -> 200
{
  "version": 7,
  "updatedAt": "2026-08-25T10:00:00Z",
  "items": [
    { "id": "...", "code": "600519", "displayCode": "600519", "name": "贵州茅台",
      "chart": "pe", "metric": "pe_ttm", "years": 20, "sourceTemplateId": "moat", "sortOrder": 0 }
  ]
}

// PUT /api/v1/me/dashboard  (body 带 baseVersion)
{ "baseVersion": 7, "items": [ ... ] }
// -> 200 { "version": 8 }  或  409 { "error": "version_conflict", "current": { version, items } }
```

### 5.3 自定义模版（配额受 entitlements 限制）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/me/templates` | 用户自定义模版列表 |
| POST | `/api/v1/me/templates` | 新建（校验 `max_user_templates` 配额） |
| PUT | `/api/v1/me/templates/{id}` | 更新 |
| DELETE | `/api/v1/me/templates/{id}` | 删除 |

> 官方模版（L1 的 3 个）仍是前端静态配置，**不入库**；只有用户自定义模版入 `user_templates`。

### 5.4 现有接口的影响

- **估值类接口**（`/api/v1/valuation/*`）是按股票的无状态查询，**不需要 user 维度**，本层不动。
- 只有"用户私有数据"接口需要 `get_current_user`；数据类接口保持匿名可用（或按 plan 限流，属后续）。

---

## 6. 后端落地要点

### 6.1 鉴权依赖（新增）

```python
# api/deps.py（示意，新增）
from fastapi import Depends, Request, HTTPException
from sqlalchemy.orm import Session
from src.auth import resolve_user_id_from_request  # 扩展 src.auth

def get_current_user(request: Request, db: Session = Depends(get_db)):
    user_id = resolve_user_id_from_request(request)  # 校验 session token -> user_id
    if not user_id:
        raise HTTPException(status_code=401, detail="unauthorized")
    user = UserService(db).get(user_id)
    if not user or user.status != "active":
        raise HTTPException(status_code=401, detail="unauthorized")
    return user
```

- `src.auth` 扩展：`verify_session` 之外新增按 token 反查 `user_id`；`single` 模式下恒返回 `default_user`。
- 现有 `AuthMiddleware` 保留（粗粒度网关）；细粒度用户隔离由 `get_current_user` + service 层查询过滤实现。

### 6.2 Service 分层

沿用现有 `src/services/*` 模式，新增：`UserService`、`AuthService`（含 SMS 频控）、`DashboardService`、`UserTemplateService`、`EntitlementService`。所有查询强制带 `user_id` 过滤。

### 6.3 迁移接口（首次登录导入本地看板）

- 前端在登录成功后，若本地 `localStorage`(`dsa:myDashboard`) 非空且服务端看板为空/更旧，调用 `POST /me/dashboard/import`。
- 服务端做**幂等去重**（同 `code|chart|metric` 视为同一项），导入后返回合并结果。

---

## 7. 前端改造（`apps/dsa-web`）

### 7.1 认证

- 扩展 `AuthContext`：`status`/`me` 增加 `user`（id/nickname/plan）；新增 `register`、`sendSmsCode`。
- 新增/改造 `LoginPage`：手机号 + 验证码流程（倒计时、频控提示，复用现有 429 处理）。
- `authApi` 增加 `sendSmsCode` / `register` / `getMe`。

### 7.2 看板从 localStorage → 服务端

- 把 `utils/myDashboard.ts` 从"直接读写 localStorage"改造为**带远端同步的 store**：
  - 未登录：仅本地（保持现状，L1 无缝可用）。
  - 已登录：以服务端为准；本地作缓存；写操作去抖后 `PUT`，处理 `409` 冲突刷新。
- 保留现有 `MY_DASHBOARD_CHANGED_EVENT` 语义，让 `MyDashboardPage` / `ValuationPage` 的 `AddToDashboardButton` **无需改动**即可继续工作（同步逻辑收敛在 store 内部）。
- 新增登录后的一次性本地→云端迁移调用（6.3）。

### 7.3 账号相关 UI

- 用户菜单（昵称/头像/登出）、登录设备管理、（预留）会员状态入口。

---

## 8. 安全设计

- 密码/验证码：`user_credentials.secret_hash` 用 argon2id 或 bcrypt；验证码存**哈希**，不存明文。
- 会话 token：随机 ≥256bit，DB 只存 `token_hash`；Cookie `httpOnly+Secure+SameSite=Lax`。
- 频控：短信发送按手机号+IP 双维度限流（如 60s/次、每日上限）；登录失败限流（沿用现有 429）。
- CSRF：Cookie 鉴权接口加 CSRF 校验或 `SameSite` + 双提交 token。
- 越权：所有用户数据查询强制 `user_id` 过滤；接口层禁止用请求体里的 `user_id` 覆盖当前用户。
- 传输：全站 HTTPS。

---

## 9. 合规（上线前必须）

> 账号体系一旦收集个人信息，合规从"建议"变"硬门槛"。

- **《个人信息保护法》(PIPL)**：明示收集范围与目的、单独同意、提供注销账号与删除数据能力（需实现"注销账号"链路）。
- **实名制**：手机号登录本身满足；如涉及金融相关增值服务需评估更强实名要求。
- **用户协议 + 隐私政策**：注册前展示并同意，缺一不可。
- **ICP 备案**：域名/服务器需完成备案（境内）。
- **数据安全**：敏感字段加密存储、访问审计、最小权限。
- 建议上线前请法务/合规过一遍注册流程与数据留存策略。

---

## 10. 部署与运维

- **数据库**：生产用 PostgreSQL（SQLAlchemy 抽象，迁移平滑）；用 **Alembic** 管理 schema 迁移（新增表都走迁移脚本）。
- **缓存/频控**：引入 Redis 存短信验证码、频控计数、可选会话缓存。
- **配置项（新增环境变量，示意）**：
  - `AUTH_MODE=single|saas`
  - `DATABASE_URL=postgresql://...`
  - `REDIS_URL=redis://...`
  - `SMS_PROVIDER` / `SMS_ACCESS_KEY` / `SMS_SIGN`（短信服务商，如阿里云/腾讯云）
  - `SESSION_TTL_DAYS`、`WECHAT_APP_ID`/`WECHAT_APP_SECRET`（M3）
- **兼容**：`single` 模式下不依赖 Redis/短信，OSS 自部署零改动可用。

---

## 11. 分期交付

| 里程碑 | 内容 | 依赖 |
|------|------|------|
| **M0 多用户地基** | users/credentials/sessions 表 + `AUTH_MODE` 开关 + `get_current_user` + `/auth/status` 扩展；`single` 模式兼容 | Alembic、DB |
| **M1 看板云端同步** | dashboards/dashboard_items 表 + `/me/dashboard` GET/PUT/import + 前端 store 改造 + 迁移逻辑 | M0 |
| **M2 自定义模版云端** | user_templates 表 + `/me/templates` CRUD + 配额校验（打通 L1 子阶段 1.3） | M0、M1 |
| **M3 微信登录 + 会员预留** | user_oauth + 微信 OAuth + user_entitlements 读取入口 + 手机验证码短信服务商对接 | M0 |

> M0+M1 即可实现"看板跨设备"的核心价值；M2 打通自定义模版；M3 面向获客与商业化。

---

## 12. 验收标准

- 同一账号在两台设备登录，看板增删/拖拽在数秒内一致（服务端为准）。
- 未登录用户本地生成的看板，在首次登录后被正确迁移到服务端且不丢、不重复。
- 并发改动触发 `409` 时前端按服务端刷新，无数据错乱。
- 所有用户私有接口越权访问（伪造他人 `user_id`）均被拒绝。
- `single` 模式（自部署）行为与现状一致，OSS 用户无感。
- 注册流程包含用户协议/隐私政策同意，且提供账号注销入口。

---

## 13. 风险与待确认

1. **架构改动面**：从单用户到多用户涉及鉴权与数据层重构，是本产品迄今最重的一层，需排足工期与回归测试。
2. 登录方式首发选型：仅手机验证码，还是同时上邮箱密码？微信登录放 M3 还是提前？
3. 看板同步冲突：MVP 用整体 last-write-wins 是否可接受？还是首发就做条目级合并？
4. 数据库：是否本层就切 PostgreSQL，还是先在 SQLite 上跑通再切？（建议直接上 PostgreSQL 以免二次迁移）
5. 短信服务商与成本、ICP 备案与合规文档的负责人与时间表。
6. 会员配额的免费/付费初始数值（`max_dashboard_items` 等）需与商业化方案对齐。

---

*本文档为 L2 实现级设计参考。代码片段为规范示意，具体实现以工程约定为准。账号体系上线前须完成 PIPL/隐私政策/备案等合规事项。*
